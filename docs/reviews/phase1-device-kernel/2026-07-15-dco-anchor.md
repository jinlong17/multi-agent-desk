# DCO anchor correction note

The Phase 1 implementation and ship-receipt PRs were merged with GitHub's
squash action before the merge body included the repository's required
`Signed-off-by` trailer. The resulting main commits are retained as
historical, explicitly grandfathered DCO exceptions in the next governance
correction. This signed anchor commit establishes the new policy-effective
point; the follow-up correction will set the exact full SHA and reasons without
changing product code or release state.
