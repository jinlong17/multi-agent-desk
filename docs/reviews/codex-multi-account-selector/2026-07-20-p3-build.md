# P3 build report: Codex explicit multi-account selector

- Date: 2026-07-20
- Executor: `feature-build / P3`
- Branch: `codex/provider/codex-multi-account-selector`
- Base commit: `1219b54`
- Result: `READY_FOR_VERIFY`

## Implemented

P3 makes the platform support boundary executable and user-visible. A shared
Provider-layer gate accepts only Linux amd64 for the selector, reports macOS as
schema-compatible but identity-acceptance-pending, and reports Windows/other
architectures unsupported. Application preview and post-reservation runtime
startup both invoke it before credential materialization or Provider spawn.

The user guide now follows the actual explicit selector flow instead of the old
raw-ID development harness. The compatibility matrix records exact Linux live
evidence separately from macOS schema smoke and Windows compile/protocol
evidence. API/test contracts name the stable typed errors and defense-in-depth
runtime check.

## Writer verification

- `go test -count=1 ./...` — pass.
- `go vet ./...` — pass.
- `go test -race -count=1 ./...` — pass.
- platform gate and reserved-runtime drift tests with `-race -count=10` — pass.
- Darwin arm64, Linux amd64, Windows amd64 builds — pass with SHA-256
  `4a0eda...d4892`, `2c53d9...121b2`, `b493fd...fd425`; compile evidence only
  for macOS/Windows.
- pnpm `10.23.0` Web TypeScript/build — pass.
- Desktop Rust fmt/check — pass.
- workflow mirrors, layout, Go format, Actions, CODEOWNERS, CI fixtures, local
  links, licenses, dashboard refresh/verify, and `project:verify` — pass.
- `git diff --check` — pass before final documentation/status refresh.

## Open gate

Independent `feature-verify / P3` and the final selector `security-review`
remain mandatory. The phase does not accept macOS/Windows runtime support,
resolve the Security Gate, or authorize push, merge, ship, packaging, or
release.

## Verification focus

Verify both platform-gate call sites, stable error propagation, zero
materialization/spawn on non-Linux descriptors, exact Linux behavior,
cross-platform documentation truthfulness, raw-ID removal from user guidance,
governance links, and all P1/P2 security invariants.
