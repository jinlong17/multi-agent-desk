# P1 build report: Codex explicit multi-account selector

- Date: 2026-07-16
- Executor: `feature-build / P1`
- Branch: `codex/provider/codex-multi-account-selector`
- Result: `READY_FOR_VERIFY`

## Implemented

The approved P1 contract is implemented without entering live P2. Migration 7
adds revision-bound enrollment confirmation and persistent one-time Session
previews. Application auth now separates official-login validation from typed
alias confirmation and Vault seal. Every Codex IPC/CLI start is routed through
daemon preview plus full confirmation; raw opaque IDs cannot bypass it.

The storage boundary validates the current Account/Profile/Credential/Device/
Workspace/Usage tuple, Vault and revocation state, compatibility fingerprints,
owner, expiry and request digest before atomically consuming the preview and
inserting the Session. Same-request recovery returns the recorded Session.

## Scope decisions retained

- Default compatibility is Linux amd64 `0.144.2` only.
- P1 reservation is terminated with a typed gated failure; no live Codex
  process or materialized Home is created.
- macOS, Windows, live A/B acceptance and final Security Review remain open.
- No Provider identity or secret field was introduced.

## Tests

Passed: full Go, vet, race, migration v6 preservation, preview authority/race/
replay/drift, auth confirmation negatives, authenticated raw-ID denial, three
OS Go builds, Web TypeScript/build, Desktop Rust fmt/check, workflow/dashboard,
layout, Actions, CODEOWNERS, fixtures, links, licenses and diff integrity.

## Verification focus

Independent `feature-verify` should inspect the migration rebuild, enrollment
state transitions, alias/revision binding, Vault seal CAS, preview transaction,
P1 terminal receipt, public CLI/IPC bypass denial, redaction, and the exact
Linux/macOS/Windows capability boundary.
