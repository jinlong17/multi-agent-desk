# P2 build report: Codex explicit multi-account selector

- Date: 2026-07-20
- Executor: `feature-build / P2`
- Branch: `codex/provider/codex-multi-account-selector`
- Base commit: `401ea2a`
- Result: `READY_FOR_VERIFY`

## Implemented

The P1 daemon preview/confirmation reservation now enters the real exact-Linux
Codex runtime through `StartReserved`. The runtime rechecks the accepted binary
and protocol fingerprints, coalesces concurrent exact replay, refuses a shared
runtime with a mismatched fingerprint, and preserves the one-Session/one-tuple
authority established by the preview transaction.

The public selector surface now includes the minimal TUI start and live Usage
refresh. Usage persistence is credential-bound and returns the same generic
Provider version, availability, observation, and staleness fields that are
stored. Administrative raw Profile ID repair remains separate from the public
selector boundary. Regression coverage includes two concurrent Accounts,
distinct Usage, reservation drift, runtime reuse, raw-ID denial, explicit
refresh targeting, and cross-Account logout isolation.

## Live acceptance

On the operator-approved Linux amd64 target, binary SHA-256
`099ab537489dcc4c5ca683e73bb3a85748ff9ce3de0e167f70abad5c34a1e91b`
ran exact Codex CLI `0.144.2`.

- `@A` and `@B` started concurrently with different Session, Provider thread,
  Account, and CredentialInstance IDs.
- Active B logout returned a conflict. Stopping B, logging out, and completing
  official B re-login created a new B CredentialInstance while A stayed
  running unchanged; B restarted successfully.
- Final Usage refresh created
  `usage_d1724a3feeeb27efc96ee0d882f59e22` for A and
  `usage_637421ad91185cb9beda4d9ab892be4b` for B. Both rows contain the correct
  distinct CredentialInstance, Provider version `0.144.2`, availability
  `available`, timestamps, and 64-character redacted digest; the two digests
  are distinct.
- Final A/B Sessions both reached `exited`. After stop and Vault lock, the
  managed credential Home contained zero materialized `auth.json` files and
  the acceptance daemon was not running.

The live run used only the existing official login and `account/usage` paths.
No hidden quota request, identity field, auth payload, or secret was persisted
in feature evidence.

## Verification matrix completed by writer

- `go test -count=1 ./...` — pass.
- `go vet ./...` — pass.
- `go test -race -count=10` for reserved-start A/B isolation/drift and TUI
  explicit-target regressions — pass.
- Darwin arm64, Linux amd64, and Windows amd64 Go builds — pass. This is only
  build evidence for macOS/Windows, not live Provider acceptance.
- pnpm `10.23.0`: `web:check`, `web:build` — pass.
- `desktop:fmt-check`, `desktop:check` — pass.
- `workflow:generate`, `workflow:verify`, dashboard refresh/verify, and
  `project:verify` — pass with dashboard focus bound to `READY_FOR_VERIFY`.
- `git diff --check` — pass before documentation refresh.

## Open gates

- Independent P2 `feature-verify` has not yet issued its verdict.
- macOS distinct-account and real Windows Codex acceptance remain P3 gates.
- Final selector Security Review, platform/user documentation, compatibility
  dashboard closure, and release/ship authorization remain open.

## Verification focus

Independently trace the preview-to-reserved-Session handoff, exact replay,
post-reservation drift, shared-runtime fingerprint, A/B runtime and Usage
isolation, alias-scoped logout/re-login, public raw-ID denial, redaction,
materialization cleanup, and truthful Linux/macOS/Windows capability boundary.
