# Development log: Phase 4a Control Plane Core

## Status Panel

| Field | Value |
|---|---|
| Workflow | `FEATURE_DEV` |
| Target | `phase4a-control-plane-core` |
| Title | `Phase 4a Control Plane Core` |
| Owner Module | `control-plane` |
| Impacted Modules | `security`, `core`, `web`, `desktop`, `project-system` |
| Current Phase | `P2 Bootstrap/passkey/recovery/session` |
| Status | `BLOCKED` |
| Executor | `Codex (GPT-5) as independent bug-verify` |
| Updated | `2026-07-23 14:54 PDT` |
| Suggested Next | `ship the scoped macOS FIFO lsof device-field receipt repair under human authorization; P2 remains BLOCKED pending the pre-existing clean-SHA Chrome/Safari/physical Touch ID scan/finalize evidence` |
| Branch / Worktree | `codex/control-plane/phase4a-core` / `/Users/jinlong/Desktop/jinlong_project/agent-deck-worktrees/phase4a-control-plane-core` |
| Plan Version | `v0.9` |
| Provider Gate | `none` |
| Security Gate | `open — bootstrap, auth/recovery, device identity/pinning, signed network requests, sync/commands, Web origin` |

## Phase Plan

| Phase | Scope | Dependencies | Acceptance | Status |
|---|---|---|---|---|
| P0 Contract freeze | Reconcile pin/fingerprint/JCS+PoP vectors, portable Vault assertion, 4a/4b/5 capability split, UUIDv7, OpenAPI/tool/license pins, threat/data/protocol docs | approved v0.4 plan; ADR 0010/0011 evidence | Go/TS vectors agree, including negative `storageMode` and `storageAssertionDigest` mutations; authoritative docs and versioned capability reservations agree; exact WebAuthn/codegen/kin-openapi/TS/UUID tool graphs and licenses pass; no product behavior | `verified` |
| P1 Server/storage/OpenAPI foundation | server lifecycle/config/health/version; full v0.7 OpenAPI 3.0.3; generated Go server/client + TS types; exhaustive TS runtime client; request/ID/cursor/idempotency/error middleware | P0 independently verified; v0.7 independently approved | clean temp generation byte-matches twice; full v0.7 operation/DTO/migration contract is present and all legacy shapes reject; only health/ready/version runtime handlers are mounted; hostile input, three-platform and tool-license gates pass; no P2+ state | `verified` |
| P2 Bootstrap/passkey/recovery/session | migration 0008 plus verified v7 backup/restore; immutable canonical server-origin-bound remote identity/envelope/AAD/actors; exact WebAuthn binary DTO/counter CAS/passkey deletion; restart-safe CSRF; globally unique normalized-key/canonical-request auth idempotency with thirteen closed operations and secret-nonreplay receipts; item-authoritative session revisions/coalesced idle touch; `__Host-mad_session`; bootstrap/recovery/session lifecycle | P1 independently verified; v0.9 independent review required | backup permissions/recovery, cross-server rejection, origin/envelope/CAS/crash/actor/receipt, exact 0/equal/regressed counters+session revoke, CSRF restart/corruption, global-key/canonical-JCS/cross-actor/all-route replay/lost-response, complete operation enum/generated parity, per-item revoke/touch races, recovery/secret scans, real Chrome+Safari on macOS arm64, and real Safari Touch ID/platform Passkey pass; Windows/Ubuntu compile/build and native Windows DACL pass without browser/Claude acceptance | `blocked` |
| P3 Remote Device identity/enrollment/presence/revocation | migration 0009 remote trust; distinct local remote identity; exact signed Device auth; pre-auth enrollment state/rechallenge/two-step pin activation; strict signed receipt + activation-package wrapper/final ack; typed JCS formulas; capability separation; Web real X25519; lifecycle/presence; snapshot-required only | P2 independently verified; P0 vectors | challenge/exchange restart/CAS/nonce, every payload/package/ack digest/domain/cross-type/raw-key mismatch, pending-key actor matrix, pin-before-final-ack, mapping/key replacement, boot epoch/offline, delegation/revoke, software-wrapped PoP and no snapshot implementation pass | `planned` |
| P4 Metadata projection/sync | migration 0010; exact typed RFC 8785 wire/snapshot with pageSize<=4 and 900-KiB pull budget; no Fake network Session; disabled browser Profile/local completion; separate local/server revisions; push/pull/ack/watermarks | P3 independently verified | worst-case response bounds, change/snapshot goldens and hostile continuity, no-Fake/no-local-policy leakage, Profile ready-before-enable, separate revision concurrency/restore, backup/quarantine/commit pass | `planned` |
| P5 Async Session Commands | migration 0011; canonical command digest/preallocated result Session; exact list/offer/claim/ack/result/reconcile/query/cursor DTO; append-only delivery revisions; closed outcome/reservation/proof/receipt oneOf; half-open clock/priority; dedicated RemoteCommandService; stable outcomes/concurrency | P4 independently verified; Phase 1 local services | list-no-claim -> claim-attempt/lease -> local reserved receipt -> ack contiguous server cursor goldens, every delivery/receipt revision and derived idempotency, strict legacy rejection, attempts/retention/GC, per-kind restart proof, no local_committed->ambiguous, races/shutdown/concurrency and acquire/release typed-unsupported pass | `planned` |
| P6 Web metadata UI + integration/security handoff | enrollment list + bounded Overview; exact Usage units/ProfileConflict; browser-safe crypto/IndexedDB/pins; auth DTO/PWA no-cache; metadata pages; real macOS browser+Desktop render; polling/recovery/logout privacy | P5 independently verified | all retained v0.7 states/journeys plus v0.9 P2 auth DTOs; exact Chrome/Safari macOS-arm64 receipts, Safari real Touch ID Passkey/X25519, Tauri launched render, WCAG/responsive, dependency/project/secret/security gates pass; Windows/Ubuntu compile/build only; no Terminal/Approval/Grant; Security Gate reviewer-owned | `planned` |

Each `feature-build` owns exactly one approved row above and must stop at
`READY_FOR_VERIFY`. An independent `feature-verify` must set that phase
`VERIFIED` before the next row can begin. `feature-review` may split P6 into P6A
and P6B without broadening scope.

## Evidence Ledger

| Time | Phase | Command/evidence | Result | Artifact |
|---|---|---|---|---|
| 2026-07-21 01:05 PDT | PLAN | `git rev-parse origin/main`; `git rev-parse HEAD`; worktree/branch/status inventory | baseline is `origin/main@e3578390a23ddcf805ceb0bad24b1c41d36977fb`; intake checkout is clean at `71e0448de1624ae3c00cec82f800d0e5425a4dc5` on the operator-created `codex/control-plane/phase4a-core` worktree | Git facts; committed feature brief |
| 2026-07-21 01:15 PDT | PLAN | complete read of AGENTS/CLAUDE, implementation plan, workflow, module registry, feature-plan role, feature brief, ADR 0002/0003/0005/0008/0010/0011, threat model, feature template, current domain/migrations/Web/server/protocol scaffolds | owner is unambiguously `control-plane`; impacts `security/core/web/desktop/project-system`; existing prefixed IDs, portable Vault v1, Usage model, empty server/OpenAPI/Web boundaries recorded | `design.md`; `api.md`; `test.md` |
| 2026-07-21 01:30 PDT | P0 research | GitHub release/tag API and upstream manifests/licenses for `go-webauthn v0.17.4`; upstream release/manifests for `oapi-codegen v2.8.0`; npm registry metadata for `openapi-typescript 7.13.0`; repository Go/Node/pnpm pins | WebAuthn tag is PGP/GitHub verified, 2026-05-22, BSD-3-Clause, Go 1.25/toolchain 1.26.3; oapi-codegen is Apache-2.0/Go 1.25; openapi-typescript is MIT/Node-compatible; repo Go 1.26.5/Node 24 can proceed, subject to full locked graph license gates during build | exact pins in `api.md`; upstream URLs recorded in plan handoff evidence |
| 2026-07-21 01:40 PDT | PLAN | inspect `docs/spikes/e2ee/PROTOCOL.md`, Go/TS vector implementations, and current pin/attestation output | current vector stores full digest but displays eight full-hex groups and current attestation lacks both full digests; P0 authoritative/vector correction is mandatory | `design.md` P0 reconciliation; `test.md` P0 vector gate |
| 2026-07-21 01:49 PDT | PLAN | freeze v0.1 design/API/test/phase plan and run `git diff --check` | four canonical planning artifacts created; status is `NEEDS_REVIEW`; no product code, migration, dashboard, implementation plan, ADR, commit, or push performed | this feature directory |
| 2026-07-21 01:55 PDT | PLAN | bundled Node/pnpm `npm run project:verify`; `npm run ci:verify`; `git diff --check` | workflow/dashboard generation and verification, Actions/CODEOWNERS/fixtures/links/licenses, and diff integrity all pass; generated dashboard facts remain ignored | command output; generated dashboard state |
| 2026-07-21 02:19 PDT | PLAN v0.2 | read persisted feature-review v1; reconcile seven findings across brief/design/API/test without changing P0-P6 order | exact envelope/bootstrap rotate, TS runtime, CSRF/recovery, command receipt, watermark/diff, capability/enrollment, and mapping/snapshot contracts frozen; resubmitted `NEEDS_REVIEW` | `docs/reviews/phase4a-control-plane-core/2026-07-21-feature-review-v1.md`; v0.2 feature artifacts |
| 2026-07-21 02:42 PDT | PLAN v0.3 | read persisted feature-review v2 and all five current planning/state artifacts; reconcile four material findings without changing the P0-P6 sequence | moved executable 0008/envelope/mapping/bootstrap actors into P2; froze canonical full-base/full-next/patch sync wire and history behavior; froze reserved-only command attempt rebind/later-state reconcile; removed stale activation-material test wording and added both storage PoP negatives | `docs/reviews/phase4a-control-plane-core/2026-07-21-feature-review-v2.md`; v0.3 feature artifacts |
| 2026-07-21 02:53 PDT | PLAN v0.4 | read persisted feature-review v3 and reconcile its single snapshot finding across the five planning/state artifacts | made the authenticated/enrolled target Device an out-of-band prerequisite, changed snapshot topology to Account-first, and froze exact RFC 8785 manifest/page/final digest, continuity, expiry, replay, and commit contracts | `docs/reviews/phase4a-control-plane-core/2026-07-21-feature-review-v3.md`; v0.4 feature artifacts |
| 2026-07-21 09:26 PDT | P0 BUILD | `node docs/spikes/e2ee/verify.mjs`; direct `go test ./...`, Go/TypeScript vector runs, byte comparison, `node --check`; `npm run project:verify`; `npm run ci:verify`; `git diff --check`; secret-pattern, changed-path allowlist, and runtime `openapi-fetch` scans | pass: canonical verifier SHA-256 `34da114390dec3e6c089313fe0e7c44dcb46f94af57458ee85004e01c0fa5ca8`; direct Go/TypeScript output is byte-identical with SHA-256 `53c206b6d9450028f57e50246ece147cf037281f2f6ff741d082a29fe8c7bfa5`; both attestation digest mutations, raw-key digest mismatches, storage assertion negatives, six-group pin presentation/truncation negatives, and all prior pairwise E2EE negatives reject; structural, link, license, diff, secret, and scope gates pass | P0 authoritative docs and portable vectors; command output; no product package, migration, dependency lock, dashboard judgment, commit, or push change |
| 2026-07-21 09:37 PDT | P0 VERIFY | independent `node docs/spikes/e2ee/verify.mjs`; direct Go/TypeScript runs, `cmp`, SHA-256 and field audit; `go test -count=1 ./...`; `node --check`; `npm run project:verify`; `npm run ci:verify`; dependency provenance/license/toolchain, cross-doc boundary, changed-path, lockfile, `openapi-fetch`, secret, and `git diff --check` scans | local positive gates pass: verifier `34da114390dec3e6c089313fe0e7c44dcb46f94af57458ee85004e01c0fa5ca8`; byte-identical output `53c206b6d9450028f57e50246ece147cf037281f2f6ff741d082a29fe8c7bfa5`; scope/dependency/docs/repository checks pass | `docs/reviews/phase4a-control-plane-core/2026-07-21-p0-feature-verify.md`; local command output |
| 2026-07-21 09:37 PDT | P0 VERIFY hostile/evidence | invalid-calendar attestation vector (`2026-02-30T16:00:00Z` to `16:10:00Z`); TypeScript proof-comparison source audit; `git ls-remote` plus branch-scoped `gh run list` for `spike-e2ee.yml` | blocked: Go rejects the invalid timestamp while TypeScript accepts; TS HMAC uses non-constant-time `Buffer.equals`; no fresh Ubuntu/Windows revised-vector run exists | same verification report; clearing role is original `feature-build` P0 writer |
| 2026-07-21 09:42 PDT | P0 BUILD clearing | strict TypeScript UTC RFC3339 component/calendar and microsecond-lifetime validation; shared Go/TypeScript hostile `2026-02-30`, hour-24, leap-day boundary vectors; length-safe `timingSafeEqual` exchange-HMAC verification; wrong-content/short/long proof regressions; local vector/direct parity gates | local defects cleared: verifier `55bff1decd0b3419df4d43e32fe933e397a9167253c89f7a7d71552c178520f5`; byte-identical direct output `3ab9bbe3477ee64293d229c50b364b5c3721ae8190c6c443b33b2b585adf8dde`; P0 remains `BLOCKED` solely until one exact commit has successful native Ubuntu/macOS/Windows vector receipts | TypeScript/Go vector implementations, comparator, authoritative result-hash docs, local command output; verifier report preserved |
| 2026-07-21 09:53 PDT | P0 exact-commit CI | PR #32 exact head `37d517bd6f9f5f70c8f21a9ac8e544b5a106a993`: E2EE run `29850227870` jobs Ubuntu `88700932340`, macOS `88700932375`, Windows `88700932424`; CI run `29850228526` jobs project `88700973142`, build Ubuntu `88700973107`, macOS `88700973080`, Windows `88700973127`; Governance run `29850228178` jobs license `88700933439`, DCO `88700933431`, link `88700933374` | all 10 checks pass on the same exact commit; prior head `70dda21` Ubuntu unrelated known session flake and npmjs 403 link failure are cleared by the fresh exact-head results | GitHub Actions receipts for PR #32; current branch/head and origin agree at `37d517bd6f9f5f70c8f21a9ac8e544b5a106a993` |
| 2026-07-21 09:58 PDT | P0 VERIFY v2 local | independent current-hash harness and direct Go/TypeScript byte comparison; eight modified-vector UTC/calendar/leap/hour/fraction/lifetime probes; HMAC wrong-content/31/33-byte proof audit; `go test -count=1 ./...`; Node syntax; `npm run project:verify`; `npm run ci:verify`; five provenance HTTP checks; scope/lock/`openapi-fetch`/secret/DCO/diff scans | pass: strict time probes have exact Go/TypeScript parity; accepted cases byte-match; length-safe constant-time proof path rejects all three HMAC negatives; verifier `55bff1decd0b3419df4d43e32fe933e397a9167253c89f7a7d71552c178520f5`; direct output `3ab9bbe3477ee64293d229c50b364b5c3721ae8190c6c443b33b2b585adf8dde`; all local repository/scope/provenance gates pass | `docs/reviews/phase4a-control-plane-core/2026-07-21-p0-feature-verify-v2.md`; local command output |
| 2026-07-21 09:58 PDT | P0 VERIFY v2 remote | independent `gh pr view 32`, Actions run/job API, and E2EE log queries against local/remote/PR head `37d517bd6f9f5f70c8f21a9ac8e544b5a106a993` | pass: all three runs bind exact head; all 10 named jobs/IDs completed `success`; Ubuntu/macOS/Windows logs each emit current verifier hash and full negative-case receipt | PR #32; runs `29850227870`, `29850228526`, `29850228178`; exact job IDs in v2 report |
| 2026-07-21 11:03 PDT | PLAN v0.5 | complete re-read of repository rules/role/v0.4 artifacts; P2-P6 contract audit reconciliation; npm registry metadata query for exact browser/runtime/test dependency candidates; `git diff --check`; `npm run project:verify`; `npm run ci:verify` | diff/workflow/dashboard, Actions, CODEOWNERS, fixture, and link gates pass; license gate truthfully blocks on a P1-checkpoint pnpm dependency group with disallowed `Python-2.0`, which v0.5 assigns to P1 dependency reconciliation before verification; P0 remains verified and no P1 implementation file changed | feature brief; `design.md`; `api.md`; `test.md`; this state authority; command output |
| 2026-07-21 11:24 PDT | PLAN v0.6 | complete v5 finding-to-contract scan; `git diff --check`; `npm run project:verify`; `npm run ci:links`; `npm run ci:static`; `npm run ci:fixtures`; `npm run ci:licenses` | diff, workflow, dashboard, local links, Actions, CODEOWNERS, and fixture gates pass; license gate truthfully remains blocked only by the paused P1 dependency graph's disallowed `Python-2.0`; no implementation/generated/dependency/dashboard judgment file changed by feature-plan | v0.6 five-file planning authority; local command output |
| 2026-07-21 11:32 PDT | PLAN v0.7 | byte comparison of design/API P5 terminal-outcome blocks; exact stale-token scan; `git diff --check`; `npm run project:verify`; `npm run ci:links`; `npm run ci:static`; `npm run ci:fixtures`; `npm run ci:licenses` | design and API outcome blocks byte-match; no bare `execution_ambiguous` exists in either current authority; diff, workflow, dashboard, links, Actions, CODEOWNERS, and fixtures pass; license gate remains blocked only by the known paused-P1 `Python-2.0` graph | v0.7 five-file planning authority; local command output |
| 2026-07-21 12:22 PDT | P1 BUILD | frozen-lock install; two direct `api:verify` runs plus the scaffold regeneration gate; API/whole-graph/Go license checks; Protocol/Web check-test-build; full Go test, race, vet, build, repeated migration race, Linux amd64/Windows amd64/macOS arm64 cross-build and Windows compile probe; `project:verify`; `ci:verify`; `scaffold:verify`; DCO, diff, secret, stale-dependency, scope, and ignored-artifact audits | pass: v0.7 exposes 65 unique operations and 270 schemas; temporary Go/TS generation byte-matches checked-in artifacts every run; OpenAPI/Go/TS SHA-256 are `a02bf6839c2b7a4b85ecc7275983d32b74bd3f2a4db47bdf2b0872a8e2232545` / `4cfc1890744f50afecfd00616850b124a0cbbee0c8679f30626302592ce81383` / `2938d490c1aa9d2da4e2587606984482a4286accfd83c90843b1dffbbaa1d3c7`; only health/ready/version mount at runtime and all 62 P2+ operations return no-side-effect JSON 404; SQLite restart/concurrency/checksum/FK/idempotency/private-sidecar/verified-backup and hostile transport/config tests pass; exact pnpm override removes `argparse`/`Python-2.0`; no secret or out-of-scope plan/review/dashboard-judgment drift | P1 implementation/generated/migration/config/runtime/protocol/Web import-point files and locked dependency graph; local command output; no commit, push, merge, or P2+ runtime behavior |
| 2026-07-21 12:32 PDT | P1 VERIFY local/contract | independent complete authority/diff review; two clean-temp `api:verify` executions; schema/reference/strict-object traversal; full Go/race/vet/repeated concurrency/cancellation; native/cross compile; Protocol/Web/project/CI/license/DCO/diff/scope/dependency scans; ignored-artifact audit | local foundation, generation, storage, hostile-input, license, and no-P2-side-effect gates pass; OpenAPI has 65 operations, 270 schemas, 1337 local refs, zero missing refs, and zero loose object schemas; local Web green depends on ignored Protocol `dist`; exhaustive client lacks Enrollment pre-auth and makes required generated inputs optional | `docs/reviews/phase4a-control-plane-core/2026-07-21-p1-feature-verify.md`; local command/source evidence |
| 2026-07-21 12:32 PDT | P1 VERIFY remote | `gh pr view 32`; run/job API and exact native logs for PR head `467067002f0b14716ef0720cb0f6ed455458936c`, CI run `29861479555` | blocked: macOS job `88739004136` fails clean Web TS resolution of `@multi-agent-desk/protocol`; Windows job `88739004154` fails the Control Plane suite at current-logon owner verification; E2EE Ubuntu/macOS/Windows, project, license, DCO, and link pass; Ubuntu build `88739004182` remained in progress at verdict time | PR #32 exact-head check/log receipts; same verification report |
| 2026-07-21 13:06 PDT | P1 BUILD clearing | reproduce clean TS2307; remove workspace `node_modules` and every `dist`/`target`, frozen-lock reinstall, then Web check/build before Protocol emits `dist`; two clean-temp `api:verify` runs; Protocol check/8-test/build; full Go/race/vet/20x concurrency, native and Linux/Windows/macOS compile; clean-start `scaffold:verify`; project/CI/API/Go/full-graph license, DCO, secret, scope, diff, and artifact scans | all four findings cleared locally: Protocol workspace source exports resolve clean check/build without hidden output; cross-platform fixtures and newly created DB files use the unchanged current-logon/protected single-principal private-path helpers, with native-Windows DACL positive/unprotected-negative tests and Windows compile probe; all seven EnrollmentPreAuth operations have the exact four signed headers and the runtime client composes Enrollment authorization, browser cookie/conditional CSRF, collision rejection, and operation coverage; generated required body/path plus required-query generic fixtures fail compilation when omitted and missing runtime paths reject; OpenAPI/Go/TS SHA-256 are `7a7eadfbe5ebe6ad8a7fe6abbf46de51a84780eb62710f6b6130cbebcaaa1450` / `b0c321addc26474e1e7c3cf7ec505b6c642acf47a692a118f9f3e391a83ce652` / `746d7dc102f516f39a281a3fb3ed60f1c3c05d9a1989e681204483becc66d896`; verifier report remains unchanged and P2 is untouched | P1 implementation/OpenAPI/generated/tests/package-topology files; clean local command output; exact-head native Windows/macOS rerun remains verifier-owned after coordinator commit/push |
| 2026-07-21 13:16 PDT | P1 VERIFY v2 | independent exact-head clearing-diff/source audit; truly fresh no-dependency/no-output clone with frozen install and no-Protocol-dist Web check/build; Protocol 8-test runtime/type negatives; deterministic generation, 65-operation/270-schema contract traversal, full Go/hostile/foundation-only checks, race/vet/20x concurrency, project/CI/diff gates; exact PR #32 job/log binding | prior Findings 1-4 are cleared, including native Windows owner/DACL behavior, but P1 remains blocked: exact-head Windows job `88749101666` fails `TestStorePriorSchemaBacksUpAndUpgrades` because `Store.Backup` opens the protected backup read-only and `Sync` returns `Access is denied`; the other 9/10 exact-head checks pass | `docs/reviews/phase4a-control-plane-core/2026-07-21-p1-feature-verify-v2.md`; PR #32 run `29864469590`; local/fresh-clone command and source evidence |
| 2026-07-21 13:25 PDT | P1 BUILD clearing v2 | change the verified-backup durability handle from read-only to the minimum necessary `O_RDWR`; strengthen `TestStorePriorSchemaBacksUpAndUpgrades` to open and sync the protected backup read-write, close it, and re-verify its private-file contract; focused 1x/20x plus Windows compile probe; full Go/race/vet/storage 20x; native/Linux amd64/Windows amd64/macOS arm64 builds; no-Protocol-dist Web check/build; Protocol check/8-test/build; deterministic API twice, API licenses, project/CI/layout/format/DCO/diff/secret/scope/artifact gates | pass: the original Windows failure path now uses a writable sync handle without removing `Sync`, weakening current-logon protected single-principal DACL enforcement, or bypassing SQLite integrity/foreign-key/data verification; all four v1 finding regressions remain green; verifier v2 report and P2 are unchanged; exact-head native Windows and 10/10 protected checks remain independent verifier/coordinator evidence | `internal/controlplane/store.go`; `internal/controlplane/store_test.go`; this state authority; local command output |
| 2026-07-21 13:50 PDT | P1 BUILD clearing v3 | reproduce exact-head `3b2aa28` Windows Server 2025 / Node 24.18 `spawnSync pnpm ENOENT`; execute the locked local `openapi-typescript` CLI directly with `process.execPath`; for Windows-only pnpm license operations resolve the action-provided absolute `PNPM_HOME` to the canonical sibling `pnpm/bin/pnpm.cjs`, validate package metadata/path/file containment, and invoke it with Node; keep POSIX pnpm direct; route all child processes through argument arrays and forced `shell:false`; add six execution/construction/fail-closed regressions to the protected `api:verify` gate | pass: the Windows regression actually starts the resolved fixture CLI through Node and round-trips a metacharacter-bearing argument unchanged; missing/relative `PNPM_HOME` and false pnpm metadata reject; deterministic API verification passes repeatedly with unchanged generated artifacts; API/full-graph licenses, complete scaffold including Desktop release build, project/CI, Go full/race/vet/three-platform builds, Protocol 8/8, DCO/diff/secret/scope/no-shell/artifact gates pass; P2 and verifier reports remain unchanged; fresh exact-head native Windows evidence remains independent | `package.json`; `scripts/api/{generate.mjs,process-runner.mjs,process-runner.test.mjs,verify-tool-licenses.mjs}`; `scripts/ci/verify-licenses.mjs`; this state authority; local command output |
| 2026-07-21 14:15 PDT | P1 BUILD clearing v4 | reproduce exact job `88759763597` and inspect the pinned `pnpm/action-setup@d15e628` self-update layout: bootstrap sibling remains pnpm 11.1.1 while the active 10.23.0 install is recorded under `PNPM_HOME/global/v11/<install-id>` and canonicalizes into the versioned `PNPM_HOME/store`; replace the stale-sibling assumption with a no-shell resolver that requires the active `PNPM_HOME/bin/pnpm.cmd` layout, scans only exact global install records, realpath-deduplicates candidates, enforces canonical trusted-root containment plus the repository's exact package-manager pin/name/version/bin/file metadata, and executes the unique CLI with `process.execPath` and an argument array | pass: the protected fixture now models stale sibling v11 plus active shim/global/store v10 and actually executes the resolved v10 CLI through Node; missing/relative home, wrong active metadata, trusted-root escape, and two distinct canonical candidates fail closed; focused 8/8, repeated deterministic API generation, API/full-graph licenses, frozen install, complete scaffold/Desktop release build, project/CI, Go full/race/vet/three-platform builds, Protocol 8/8, DCO/diff/no-shell/scope/artifact gates pass locally; no `.cmd`, shell, or pnpm v11 execution path exists; P2 and verifier reports remain unchanged; fresh exact-head native Windows evidence remains independent | `scripts/api/process-runner.mjs`; `scripts/api/process-runner.test.mjs`; this state authority; local command output and pinned-action layout evidence |
| 2026-07-21 14:25 PDT | P1 VERIFY v3 | independent review of the complete v2-v4 clearing chain and final exact head; process-runner pin/realpath/uniqueness/no-shell audit; runner 8/8, deterministic API/license, Protocol 8/8, clean Web, project, full Go/race/vet/20x backup/concurrency regressions; exact Windows log inspection through Go, API, license, Web, and Desktop scaffold; bind all PR #32 checks to one SHA | `VERIFIED`: v1 Findings 1-4, v2 Windows backup durability, and both v3 Windows tool-layout findings are closed; exact-head native Windows owner/DACL, prior-schema backup, runner, generator, licenses, and full scaffold pass; all 10/10 protected checks succeed at `6bbad01f17db7a40f0dc43bc45a41738f302640b`; P2 remains untouched and Security Gate open | `docs/reviews/phase4a-control-plane-core/2026-07-21-p1-feature-verify-v3.md`; runs `29869408994`, `29869408966`, `29869409039`; local and fresh-clone command evidence |
| 2026-07-21 16:39 PDT | P2 BUILD | full Go test/vet and focused race suites; real bootstrap-to-activation/login/UV/recovery/replacement integration; deterministic API and Protocol tests; native macOS plus Linux/Windows compile gates; Web/Desktop builds; project/CI/license/diff/secret/scope/artifact audits | implementation and all local/compile gates pass: exact v7 backup/restore and migration 0008; Vault-backed Ed25519/X25519 identity, canonical origin/envelope/AAD and create/open/CAS prepare/prove/verify/activate flow; process lock; bootstrap token and receipt; bounded single-use WebAuthn ceremonies with memory-only server ephemeral private key and restart invalidation; Passkey counter CAS/regression revocation; Argon2id recovery; restricted recovery replacement; session/cookie/CSRF/Origin controls; accessible memory-only Web UI. P2 remains blocked on three normative ambiguities and the operator's macOS-first support revision, so no browser/platform acceptance is claimed | P2 implementation under `apps/web`, `cmd/multidesk*`, `internal/{app,controlplane,crypto,storage,transport,vault}`, `migrations/{device,server}`, Go module graph, and local command output; no review report, dashboard judgment, commit, push, merge, or P3+ implementation |
| 2026-07-22 00:34 PDT | PLAN v0.8 | complete P2 checkpoint/source and five-artifact consistency audit; exact endpoint/browser/Claude stale-wording scans; `git diff --check`; bundled Node `npm run project:verify`; `npm run ci:links`; `npm run ci:static`; `npm run ci:fixtures`; bundled Node/Cargo `npm run ci:licenses` | pass: workflow/dashboard, local links, Actions, CODEOWNERS, fixtures, and pnpm/Cargo license graphs verify; only the five authorized planning/state files are dirty; v0.8 closes CSRF restart, one-time idempotency, session revision/touch, macOS Chrome/Safari/Touch ID, Windows/Ubuntu CI-only, and Claude subscription-PTY boundaries; no implementation or generated diff exists | feature brief; `design.md`; `api.md`; `test.md`; this state authority; local command output |
| 2026-07-22 00:44 PDT | P2 REVIEW v8 | complete v0.8 authority and exact-head/checkpoint audit; cross-compared CSRF, thirteen-route idempotency, session revision/touch, platform, rollback, generated-contract, and P3+ isolation rules; ran structural/link/license checks and queried exact-head PR checks | `REVISE`; macOS-first, CSRF, one-time-secret policy, item revision, rollback, and checkpoint scope are sound, but idempotency scope/request formulas contradict the mandatory test and the strict stored-operation discriminator omits four ceremony-begin values | `docs/reviews/phase4a-control-plane-core/2026-07-22-feature-review-v8.md`; exact head `8de93d53b46d694dcf981a541588785a88e77a32`; PR #32 10/10; no plan/implementation/generated/dashboard/Git write |
| 2026-07-22 00:59 PDT | PLAN v0.9 | full Feature Review v8 read; exact five-artifact formula/enum reconciliation; automated API endpoint-to-operation and three-domain cross-check; stale P2 scope/raw-body scan; `git diff --check`; `npm run workflow:verify`; `npm run ci:links`; `npm run ci:static`; `npm run ci:fixtures`; `npm run ci:licenses` | pass: all thirteen endpoint rows equal the one closed enum; key/body/request-identity domains occur in API/design/test; no old P2 scopeDigest/raw-body formula remains; workflow, links, Actions, CODEOWNERS, fixtures, pnpm/Cargo licenses, and diff integrity pass; only five authorized plan/state files differ | feature brief; `design.md`; `api.md`; `test.md`; this state authority; local command output; no dashboard generator run |
| 2026-07-22 01:05 PDT | P2 REVIEW v9 | complete v0.9 five-authority and v8-finding review; exact global-key/request-identity/database/index/error/restart/concurrency comparison; automated thirteen-operation and stale-formula cross-check; macOS-first/Claude/CSRF/secret/revision/rollback/checkpoint/P3+ regression; structural/link/license and exact-head remote check query | `APPROVED`; both v8 findings are closed without invention: one server-global 24-hour key primary domain, one actor-bearing JCS request identity, and one complete thirteen-value migration/OpenAPI/receipt/store/test enum; all v0.8 closures remain intact | `docs/reviews/phase4a-control-plane-core/2026-07-22-feature-review-v9.md`; exact head `e8221a3b8a2ed535f21b4ab8bdf0fc3f29f74aed`; local checks; PR #32 exact-head query; no plan/implementation/generated/dependency/dashboard/Git write |
| 2026-07-22 04:56 PDT | P2 BUILD local implementation | reconciled checkpoint `fc2a38e` to approved v0.9: strict restart-safe CSRF and thirteen-operation global idempotency/secret-nonreplay lifecycle; item revisions/touch, WebAuthn/recovery/session handling; private backup/restore and Windows DACL/reparse/durability; strict OpenAPI/generated empty-body/header delta; source-gated Web revoke and deterministic embedded static runtime; fail-closed macOS receipt harness | pass locally: handler/transaction and Windows/static independent audits find no code blocker; API/Protocol/Web runtime contracts, replay/cookie/time/concurrency/private-storage/restore regressions, 17 hermetic receipt negatives, and exact static/CORS tests pass; no P3+ runtime route, Claude API-key/print/dollar/Usage-Credits, Linux-browser, or Windows-browser acceptance was added | P2 implementation/migration/OpenAPI/generated/Protocol/Web/storage/CI/receipt files; current uncommitted snapshot on `4fa86ff2d5ba6afdacbabea3bd6f20fe9fc2d580`; no commit/push/merge or live-browser receipt |
| 2026-07-22 04:56 PDT | P2 BUILD local gates | `pnpm install --frozen-lockfile`; `go mod verify`; `go test -count=1 ./...`; `go vet ./...`; `go test -race -count=1 ./internal/controlplane ./internal/storage ./internal/transport`; `go build ./cmd/...`; CGO-disabled macOS arm64/Linux amd64/Windows amd64 CLI/server builds and Windows controlplane/storage test compilation; API verify/licenses; Protocol check/16-test/build; Web check/8-test/build/embed; receipt 17-test; `project:verify`; `ci:verify`; `scaffold:verify`; diff/format/scope/secret/artifact scans | all local gates pass; focused race: controlplane 89.259s, storage 20.236s, transport 1.040s; exact 7-check/15-action CI contract and negative fixtures pass; pnpm/Cargo license graphs pass; deterministic Web assets remain `index-CAvtSe3M.css` and `index-HSlD1TlC.js`; dashboard judgment file unchanged | local command output; independent handler audit PASS; independent Web/receipt third review PASS; independent Windows local/cross-compile audit PASS |
| 2026-07-22 04:56 PDT | P2 BUILD freeze boundary | inspected lifecycle ordering after all local gates | `BLOCKED`, not `READY_FOR_VERIFY`: the dirty snapshot has no exact implementation SHA, so native Windows Actions and final browser binaries cannot yet bind one commit; real Chrome and Safari journeys plus physical Safari Touch ID/platform Passkey remain unexecuted. The receipt harness intentionally rejects dirty worktrees and automation/WebDriver/emulation. Production P6 revoke control remains disabled pending independent P2 verification | coordinator must create/push the implementation commit, run the exact-head native Windows job, execute/finalize both clean-SHA browser rows, and return those receipts; writer may then append evidence only and set `READY_FOR_VERIFY` |
| 2026-07-22 06:19 PDT | P2 BUILD exact-Windows clearing v2 | inspected exact PR run `29921105390` at `ace3c15aab12a17751853099f622130a7d4491a3`, especially failed Windows job `88926540545`; reproduced its Codex materialization/enrollment failures; replaced raw chmod-only roots/homes/locks/refresh/manifest paths with the unchanged exact private-directory verifier plus create-only/atomic-replace Device primitives; made test sources and auth refreshes model exact manager-owned creation and ordinary same-file Provider truncation; split native Windows P2 acceptance so full-Go failure cannot suppress its verbose five-package and 20x receipts; bound each complete PowerShell step and `run: |` command sequence exactly so suffixes or extra commands cannot mask test failure | all local clearing gates pass: create-only has one winner across 16 writers; Device create/replace/collision regressions pass 20x and Codex materialization/official-residue/lock-recovery regressions pass 10x; Windows creation-time handle DACL and no-replace versus `REPLACE_EXISTING|WRITE_THROUGH` are compile/test covered; fixed `.writer.lock.new/.replace` residues map to writer conflict and Recover fail-closes on quarantine error; focused five-package verbose and 20x migration, full Go, five-package race, vet, Windows amd64 CGO0 five-package test compile plus CLI/server build, deterministic API, Protocol 16/16, Web 8/8/build/assets, project/CI 7-check/15-action/17-receipt, exact-step masking/extra-command negatives, links/licenses/format/diff/eol gates pass | run/job receipts; Device/Codex/CI implementation and tests; local command output. Status remains `BLOCKED`: this clearing has no committed/pushed SHA and therefore no fresh native Windows receipt; clean-SHA Chrome/Safari and physical Safari Touch ID receipts also remain outstanding |
| 2026-07-22 06:58 PDT | P2 BUILD exact-Windows clearing v3 | isolated the remaining Storage and Control Plane concurrent-open failure to SQLite `-journal`/`-wal`/`-shm` TOCTOU: one opener can observe a sibling-created sidecar before exact Windows protection, while SQLite can also remove that same ephemeral sidecar between `Lstat` and verify/protect; added sidecar-only bounded exact-wait/vanish handling without changing stable database/directory waiters or the exact current-logon-plus-SYSTEM DACL; made both concurrency regressions drain both results and register every successful store cleanup before assertions | final focused Storage/Control Plane tests pass; both concurrent-open regressions pass 20x; final two-package race passes (`storage` 20.689s, `controlplane` 91.575s); coordinator-run final `go test -count=1 ./...`, `go vet ./...`, `npm run project:verify`, `npm run ci:verify`, and `git diff --check` pass; Windows amd64 CGO0 Storage/Control Plane test compilation passes. Windows-only regressions deterministically cover unsafe-to-exact convergence, unsafe-observed-then-vanished not-preexisting, post-open verify/protect disappearance, and persistent unsafe content/DACL preservation; existing main-file/directory negatives remain unchanged | `internal/storage/{store.go,store_test.go,schema_v7_backup_windows.go,schema_v7_backup_unix.go,privatefs_windows_test.go}`; `internal/controlplane/{store.go,store_test.go,privatefs_windows_test.go}`; this state authority; no commit/push/merge. Status remains `BLOCKED` until a fresh exact-head native Windows receipt plus the clean-SHA Chrome/Safari and physical Safari Touch ID receipts exist |
| 2026-07-22 14:46 PDT | P2 receipt BUGFIX | Node `v24.11.1`; `node --test scripts/acceptance/p2-browser-receipt.test.mjs`; `npm run acceptance:p2-browser:test`; `node --check scripts/acceptance/p2-browser-receipt.mjs`; `node --check scripts/acceptance/p2-browser-receipt.test.mjs`; `npm run project:verify`; `npm run ci:verify`; `git diff --check` | pass: focused and packaged receipt suites are 21/21; real delayed keep-alive HTTPS proves `response.socket === null` at user `end` while the response-callback-captured TLSSocket still proves authorized direct loopback, peer raw, and exact leaf hash; missing socket, unauthorized, non-loopback, missing peer raw, leaf mismatch, and unknown-CA-before-response all reject; workflow/dashboard, 7-check/15-action CI contracts, fixtures, 322 links, 6 pnpm license groups, 418 Cargo licenses, syntax, and diff integrity pass | `scripts/acceptance/p2-browser-receipt.mjs`; `scripts/acceptance/p2-browser-receipt.test.mjs`; this state authority; no receipt/token output, product TLS relaxation, commit, push, or PR |
| 2026-07-22 14:51 PDT | P2 receipt BUG VERIFY | Node `v24.11.1`; focused positive and negative `--test-name-pattern` runs; full direct and packaged 21-test acceptance runs; Node syntax checks; `npm run project:verify`; `npm run ci:verify`; source/diff boundary audit; `git diff --check` | `READY_TO_SHIP` for scoped bug: independently reproduced `response.socket === null` at delayed keep-alive `end`; callback-captured TLSSocket preserves authorized direct-loopback/peer-raw/exact-leaf proof; missing socket, unauthorized socket, non-loopback peer, missing peer raw, leaf mismatch, and unknown CA all reject; exact JSON 200/body/non-empty version/frozen commit checks remain fail closed; project and CI gates pass | `docs/reviews/phase4a-control-plane-core/2026-07-22-bug-verify.md`; local command/source evidence; feature P2 remains `BLOCKED` only on its separately recorded external receipts |
| 2026-07-22 14:59 PDT | P2 receipt BUG SHIP | locked the four-file verified scope; reran focused 3/3 and 1/1 TLS lifecycle negatives, packaged 21/21 receipt tests, syntax, project, CI fixture/link/license and diff gates; created DCO-signed commit `a3bd87edd18a09bd1230d348a47ba16f0747cdf8` with tree `330d7d1fab0b286f85787d340afb5162d045e993`; pushed it to `origin/codex/control-plane/phase4a-core` and confirmed PR #32 at that head | scoped socket-lifecycle bugfix `SHIPPED` to the existing draft PR; no merge, tag, release, package, publication or deploy. Feature P2 remains `BLOCKED`; a parallel read-only audit additionally found that its operator-authored secret-scan booleans are not machine-bound DB/log/artifact evidence and cannot support a future P2 PASS | `docs/reviews/phase4a-control-plane-core/2026-07-22-receipt-socket-ship-receipt.md`; commit `a3bd87e`; PR #32; exact-head runs `29961072283`, `29961072238`, `29961072184` started |
| 2026-07-22 15:41 PDT | P2 machine secret-scan BUILD | replaced the three operator-authored journey scan booleans with manifest/context v2, exact writer/FIFO/cat-to-TTY process binding, full six-target/six-secret machine scans, strict public-transfer and logical/raw SQLite assertions, post-journey scan/fresh-finalize mutation binding, modernc SQLite secure-delete residue canaries, redacted failure output, and corrected 32-byte Base64url digest contracts from 42 to 43 characters | pass: receipt suite 26/26; real server/Device SQLite assertion fixture; Control Plane/Storage residue tests; full Go/vet and focused race; macOS/Linux/Windows CLI/server builds; deterministic API generation; Protocol 16/16; workflow/dashboard/Actions/CODEOWNERS/fixtures/324 links/6 pnpm groups/418 Cargo licenses/diff gates. No secret value/digest is retained by the scan receipt and no P3+ implementation was added | P2 scan implementation/tests/OpenAPI/generated artifacts/template/test plan and local command output; base head `ff9993751c150ed9494d56814bd0a7392a297237`; no commit/push/PR/live receipt. Status remains `BLOCKED` until one committed exact SHA has exact-head CI plus real Chrome/Safari, physical Safari Touch ID, and signed/fresh machine-scan final receipt |
| 2026-07-22 16:09 PDT | P2 machine secret-scan audit clearing | closed all follow-up fail-closed findings: globally regenerated exact 43/86-character RawURL contracts; bound writer argv/image/PID/uid/state/start, immutable pre-start config/key/TLS/identity vnodes, exact open DB vnodes, declared-only writable regular FDs, FIFO readers and PTYs; added `-nofollow`, secure-delete, exact migration/schema/mapping/descriptor/receipt cryptographic DB checks; removed content hashes from persisted inventories; excluded every output from scan roots; and redacted all identified secret-bearing test failures | pass: receipt suite 34/34 twice including real-migration DB tamper, open-A/replace-B, multi-`txt`, hidden log FD, root/argv/PID/input replacement, TTY access, output symlink/root, and `scanEnvironment` end-to-end negatives; `api:verify` passes with generated source/TS counts 43=`224`, 86=`11`, 42/85=`0`; full Go/vet, race (`controlplane` 100.930s, `storage` 28.409s), native/Linux-amd64/Windows-amd64 builds, Protocol check/16-test/build, project/CI/licenses/links/syntax/diff pass | implementation/tests/template/state authority; no commit, push, PR, live browser, Touch ID, or signed machine receipt. P2 truthfully remains `BLOCKED` on exact-SHA external evidence |
| 2026-07-22 16:25 PDT | P2 FIFO/PTY evidence-boundary clearing | replaced macOS-incompatible pathname-scoped FIFO holder lookup with global `lsof` canonical-real-path plus inode matching that tolerates absent device fields; added a real owner-only FIFO, live `/bin/cat` reader, and live writer regression requiring exactly both PIDs; narrowed log evidence to `fifo_to_live_tty` plus declared-process/root regular-sink counts; added exact journey `terminalRecordingUsed:false` and `terminalScrollbackCleared:true` attestations and explicitly excluded PTY-master, Terminal, OS-screen, and operator capture from the machine claim | pass: focused real FIFO test and packaged receipt suite 35/35; full Go; vet; race (`controlplane` 100.077s, `storage` 29.275s); native/Linux-amd64/Windows-amd64 builds; deterministic API; Protocol check/16-test/build; project/CI with repeated 35/35 receipt, 324 links, 6 pnpm groups, and 418 Cargo licenses; syntax and diff integrity. No absolute no-retention claim remains | receipt harness/tests/template, `test.md`, and this state authority; no commit, push, PR, live browser, Touch ID, or final receipt. P2 remains `BLOCKED` only on exact-SHA external CI/browser/Touch-ID/final-scan evidence |
| 2026-07-22 16:34 PDT | P2 strict-JSON/six-root scan clearing | made every receipt JSON boundary consume raw bytes through fatal UTF-8 decoding, restricted whitespace to JSON SP/TAB/CR/LF, built null-prototype objects, and rejected duplicate plus `__proto__`/`constructor`/`prototype` keys; added a complete two-row `scanEnvironment` success with real logical SQLite assertions and post-process-gate negatives for all six target/secret canary pairings, unreadable/non-private files, stable-read mutation/replacement, symlink/hardlink aliases, unexpected logs, empty evidence, and post-snapshot mutation | pass: focused strict JSON and 15-test six-root matrix; packaged receipt suite 50/50; invalid UTF-8, NBSP, dangerous keys, every target class, and every filesystem race/boundary reject deterministically. Full project gates follow in this same build run | receipt harness/tests/template, `test.md`, and this state authority; no commit, push, PR, P3, live browser, Touch ID, or final receipt. P2 remains `BLOCKED` on exact-SHA external evidence |
| 2026-07-22 16:38 PDT | P2 final local gate rerun | `go test -count=1 ./...`; `go vet ./...`; race for Control Plane/Storage; native, Linux amd64, and Windows amd64 command builds; `api:verify`; Protocol check/test/build; `project:verify`; `ci:verify`; Node syntax and diff integrity | pass: receipt suite 50/50 both directly and inside CI; race `controlplane` 104.705s and `storage` 31.174s; Protocol 16/16; deterministic API; workflow/dashboard; 7-check/15-action CI contracts; 324 links; 6 pnpm license groups; 418 Cargo licenses; no failure or pending local gate | local command output; P2 remains `BLOCKED` only because no committed exact-SHA CI receipt, real Chrome/Safari journeys, physical Safari Touch ID, or final machine-scan receipt exists |
| 2026-07-22 16:40 PDT | P2 detector-observation clearing | added a frozen test-only scan observation containing sanitized target evidence and per-secret counts only; strengthened all six target-class canaries to assert their own target `matchCount>0`, corresponding secret-class count `>0`, no canary material in the observation, and final fail-closed rejection independent of allowlist errors | pass: focused 15-test six-root matrix; all server DB, Device DB, runtime, log, transfer, and evidence detector assertions pass; the unexpected-log allowlist negative remains separate | receipt harness/tests/template, `test.md`, and this state authority; no secret bytes/raw path exposure, production CLI observer, commit, push, PR, P3, or external receipt |
| 2026-07-22 16:41 PDT | P2 detector-observation final gates | direct packaged acceptance; `project:verify`; `ci:verify`; Node syntax; diff integrity after the final detector assertions and documentation | pass: direct receipt 50/50 and CI receipt 50/50; workflow/dashboard, 7-check/15-action CI contracts, 324 links, 6 pnpm license groups, 418 Cargo licenses, syntax, and diff integrity all pass. The immediately prior complete gate run also remains green for full Go/vet/race, native/Linux/Windows builds, deterministic API, and Protocol 16/16 | local command output; no remaining local P2 implementation/test finding. External exact-SHA CI/browser/Touch-ID/final-scan evidence remains required |
| 2026-07-22 16:44 PDT | P2 API count truth correction | reran `npm run api:verify` and maximal-run PCRE counts over the authoritative OpenAPI source: `rg --pcre2 -o '(?<!A)A{N}(?!A)' api/openapi/control-plane-v1.yaml`; separately confirmed the receipt scanner's one 43-character detector canary is outside that API-source count | pass: current authoritative OpenAPI counts are 43=`225`, 86=`11`, 42=`0`, and 85=`0`; deterministic generation and the recursive decoded-byte/legacy-length guard pass. The 16:09 row's quoted-only 43=`224` observation omitted one embedded maximal 43-character API example and is retained only as append-only history; this correction is authoritative. The final strict-JSON, six-root, and per-target/per-secret detector audit is closed by the 16:34-16:41 evidence | this append-only correction in `dev_log.md`; no implementation/generated/test/plan/dashboard-judgment/commit/push/PR/P3 write |
| 2026-07-22 17:03 PDT | P2 Windows residue-test BUGFIX | inspected exact head `95ab131348dedde8a87d644bf0f2e6306b839b2c` and failed run `29967128042` / Windows job `89080953653`; audited the unchanged exact Windows TokenUser+LocalSystem DACL verifiers and both residue fixtures; `go test -count=1 -run 'TestP2SecretCanariesAbsentFromClosed(Database|DeviceDatabase)AndSidecars' ./internal/controlplane ./internal/storage`; `go test -count=1 ./...`; `go vet ./...`; Windows amd64 CGO0 compile of both test binaries; `npm run project:verify`; `npm run ci:verify`; `gofmt -d` and `git diff --check` | scoped bugfix `READY_FOR_VERIFY`: Control Plane now protects its existing temporary root through `privateTestDirectory(t)` and Storage gives production `Open` a nonexistent child root to create/protect; focused tests, full Go, vet, both Windows test compiles, workflow/dashboard, 7-check/15-action CI contracts, receipt 50/50, 324 links, 6 pnpm groups, 418 Cargo licenses, formatting, and diff integrity pass. Product DACL/owner verification is unchanged; native Windows has not rerun this dirty fix, and feature P2 remains `BLOCKED` on that exact-head receipt plus real Chrome/Safari/physical Touch ID/final scan evidence | `internal/controlplane/p2_secret_residue_test.go`; `internal/storage/p2_secret_residue_test.go`; this state authority; no product verifier, P3, review report, dashboard judgment, commit, push, or PR write |
| 2026-07-22 17:07 PDT | P2 Windows residue-test BUG-VERIFY | independently rechecked failed public Windows job `89080953653` at exact head `95ab131`, compared both original fixtures to the scoped repair, audited the unchanged production TokenUser+LocalSystem owner/DACL implementation, ran focused residue regressions 10x, full Go, vet, Windows amd64 CGO0 test compilation and command build, project/CI, receipt, link/license, format, and diff gates | scoped bugfix `READY_TO_SHIP`: all local checks pass with only the two fixture roots changed; production owner/DACL fail-closed behavior is unchanged. Cross-compilation is not native Windows runtime proof, so fresh exact-head Windows full-Go and P2 private-storage passes remain mandatory after coordinator commit/push; feature P2 remains `BLOCKED` on that plus real Chrome/Safari/physical Touch ID/final-scan receipts | `docs/reviews/phase4a-control-plane-core/2026-07-22-bug-verify-v2.md`; this Status Panel/Evidence Ledger/one Work Log row only; no implementation/plan/dashboard judgment/commit/push/PR write |
| 2026-07-22 18:29 PDT | P2 process-lock receipt BUGFIX | reproduced the real `collect` failure at clean head `402925fae304056e34c9bc53c1543c268329fccb`: each legitimate `multidesk-server` held the production-derived `server.sqlite.process.lock` through an O_RDWR FD, while the receipt collector treated that exact FD as undeclared. Added exact manifest/cross-row/vnode/FD/global-holder/daemon/context bindings; combined real macOS `lsof` identity/access evidence with fail-closed shared-lock contention when system `lsof 4.91` leaves BSD `flock` status blank; classified only the exact lock as scanned `runtime_residue`; removed database-prefix/backup wildcard exclusions | scoped repair `READY_FOR_VERIFY`: receipt 56/56 directly and through CI, real macOS `lsof`/exclusive-lock regression, exact Go process-lock tests, project/CI, 325 links, 6 pnpm groups, 418 Cargo licenses, Node syntax, and diff integrity pass. Unknown `server.sqlite.*`, `.bak`, and `.backup`, missing/mismatched lock fields, weak/private/link/FD/holder/daemon/context/scan evidence all reject. Production process-lock code is unchanged; P2 is not claimed complete | receipt harness/tests/template, `test.md`, and this state authority; no production lock, P3, dashboard judgment, commit, push, PR, browser launch, or long-running service |
| 2026-07-22 18:37 PDT | P2 process-lock receipt BUG-VERIFY | independently audited the five-file repair against unchanged production `process_lock.go`; reran focused 34/34 and full direct/packaged 56/56 receipt suites, ProcessLock Go 10x, real macOS blank-`lsof`/extra-holder diagnostic, project/CI, syntax/link/license/diff gates; traced runtime entry and snapshot construction line by line | `BLOCKED`: the lock/vnode/FD/flock/freeze/scan/finalize contract itself passes, but `walkPrivateTree` never records directory objects. Empty unknown `server.sqlite.*`, `.bak`, `.backup`, alternate-lock, or backup directories therefore produce no unexpected entry and no snapshot change, contradicting the documented fail-closed/full-inventory contract | `docs/reviews/phase4a-control-plane-core/2026-07-22-bug-verify-v3.md`; this Status Panel/Evidence Ledger/one Work Log row only; no implementation/test/plan/dashboard judgment/commit/push/browser/P3 write |
| 2026-07-22 18:51 PDT | P2 process-lock receipt BUG-VERIFY v4 | independently audited the directory-object repair and regressed the unchanged exact process-lock contract; verified fail-closed runtime/log/transfer/evidence directory classification, stable directory FD/path identity, metadata-only snapshot privacy, known-root exclusions, and scan-to-finalize change detection; reran focused 36/36, direct/packaged 63/63, ProcessLock Go 10x, project/CI/syntax/link/license/diff gates | scoped bugfix `READY_TO_SHIP`: v3 P1 is closed with no new P0/P1 finding; `process_lock.go` remains unchanged. Feature P2 stays `BLOCKED` on a clean exact SHA plus real macOS Chrome/Safari/physical Touch ID/fresh scan/final receipt | `docs/reviews/phase4a-control-plane-core/2026-07-22-bug-verify-v4.md`; this Status Panel/Evidence Ledger/one Work Log row only; no implementation/test/plan/dashboard judgment/commit/push/browser/P3 write |

## Risks and Blockers

- P0 is independently verified at exact commit
  `37d517bd6f9f5f70c8f21a9ac8e544b5a106a993` with fresh passing native Ubuntu,
  macOS, and Windows vector/build receipts; plan v0.9 does not reopen P0.
- The committed brief used the older OS-Vault initial-anchor wording. The plan
  reconciles it to the executable portable password-derived Vault v1 Daemon
  anchor and keeps OS wrapping/Desktop product storage in Phase 5.
- P0 replaces the older full-hex human fingerprint/attestation shape with the
  full-digest/six-group contract and preserves all prior pairwise negative
  vectors; P0 independent verification confirmed that reconciliation.
- Phase 4a breadth and concurrency risk are controlled by the seven build rows
  and mandatory independent verification between rows. P6 may be review-split,
  but scope may not expand.
- P1 removed the prior `Python-2.0` dependency edge and is independently
  verified. P2's current pnpm, Cargo, API-tool, and exact Go license gates pass.
- The Security Gate is open. Planning/tests cannot self-accept bootstrap,
  WebAuthn, recovery, pinning, signed transport, revocation, sync/command replay,
  Web origin, or residual risk.
- Exact-head run `29921105390` at `ace3c15aab12a17751853099f622130a7d4491a3`
  passed project, Ubuntu, and macOS but failed Windows job `88926540545` in the
  Codex materialization/enrollment private-boundary tests before the old
  conditional acceptance step could run. After that repair, the earlier base
  head `a2f4e35` exposed a separate concurrent SQLite sidecar lifetime/DACL
  race. Both scoped clearings and their executable local gates now pass, but the
  current machine-scan build remains intentionally `BLOCKED` on base head
  `ff999375` until the coordinator creates/pushes the new exact SHA and a fresh
  native Windows CI run proves full Go plus both
  fail-independent P2 acceptance steps. Clean-SHA Chrome, Safari, and physical
  Safari Touch ID receipts also remain mandatory.
- Plan v0.9 retains macOS arm64 as stable with real Chrome and Safari plus real
  Safari Touch ID/platform-Passkey acceptance. Windows remains Experimental
  compile/build/native-DACL CI only and Ubuntu remains compile/build CI only;
  Windows browser acceptance moves to project release Phase 6 or a later
  Windows stable-support unit. Claude is outside Phase 4a acceptance; any
  separate macOS smoke is existing-subscription interactive PTY only, with no
  API key, print mode, dollar budget, Usage Credits, or Linux/Windows claim.
- Independent handler/transaction, Windows private-storage, and Web/receipt
  audits found no remaining local code blocker. Those read-only audits are not
  formal `feature-verify` and do not replace the missing exact-head/native and
  real-browser evidence. No browser/product acceptance is claimed yet.
- The local machine-scan implementation now closes the prior evidence-design
  gap, but its passing unit/integration gates are not a live product receipt.
  Only the exact-SHA collect/journey/scan/finalize sequence can clear that
  blocker; a finding requires a full affected-row restart.
- Exact machine-scan head `95ab131348dedde8a87d644bf0f2e6306b839b2c` passed
  project, Ubuntu, and macOS in run `29967128042`, while Windows job
  `89080953653` exposed only the two new residue tests' fixture mistake: each
  treated `t.TempDir()` itself as an already-private SQLite parent even though
  Windows chmod cannot establish the required exact TokenUser+LocalSystem
  owner/DACL. The scoped fixture repair is locally `READY_FOR_VERIFY` without
  weakening the production verifier; a fresh native Windows exact-head run is
  still required after independent bug verification and coordinator commit.
- `docs/IMPLEMENTATION_PLAN.md` retains older project-wide platform/Claude
  wording and belongs to `project-system`; a separate documentation action must
  reconcile those release statements before project release Phase 6. It does
  not block independent P2 review or implementation after v0.9 approval.

## Work Log (append only)

| Time | Executor | Action | Files/commit | Result | Next |
|---|---|---|---|---|---|
| 2026-07-21 01:49 PDT | Codex (GPT-5) as feature-plan | Classified owner/impacts, re-read repository truth, reconciled audit decisions, pinned external/toolchain evidence, and froze a decision-complete v0.1 design/API/test plan with P0-P6 build/verify gates | `docs/workflow/features/phase4a-control-plane-core/{design.md,api.md,test.md,dev_log.md}`; concrete brief conflict reconciliation; no commit | `NEEDS_REVIEW`; Provider Gate none; Security Gate open; no product/dashboard/plan/ADR mutation | `feature-review` |
| 2026-07-21 02:01 PDT | Codex (GPT-5) as feature-review | Independently reviewed scope, phase ordering, crypto/auth/API/migration/sync/command/Web boundaries, rollback/tests, dependency pins, and 4a/4b/5 truthfulness; persisted ranked executable-contract findings | `docs/reviews/phase4a-control-plane-core/2026-07-21-feature-review-v1.md`; this Status Panel and one Work Log row only; no commit | `REVISE`; P0 reconciliation is executable, but the plan and P1 TypeScript-client gate require the seven recorded contract revisions before build | `feature-plan` |
| 2026-07-21 02:19 PDT | Codex (GPT-5) as feature-plan | Revised plan to v0.2 and decision-completely closed all seven v1 findings while retaining owner, gates, Phase 4a boundary, and P0-P6 order | feature brief; `docs/workflow/features/phase4a-control-plane-core/{design.md,api.md,test.md,dev_log.md}`; no product/ADR/implementation-plan/dashboard/Git write | `NEEDS_REVIEW`; Provider Gate none; Security Gate open; no self-approval | `feature-review v2` |
| 2026-07-21 02:26 PDT | Codex (GPT-5) as feature-review | Independently re-reviewed plan v0.2 and audited closure of all seven v1 findings, P0-P6 executability, security boundaries, rollback, and verification gates | `docs/reviews/phase4a-control-plane-core/2026-07-21-feature-review-v2.md`; this Status Panel and one Work Log row only; no commit | `REVISE`; P0/P1 are executable, but envelope/bootstrap phase ordering, sync digest/diff encoding, command attempt rebinding, and stale test assertions require plan revision | `feature-plan` |
| 2026-07-21 02:42 PDT | Codex (GPT-5) as feature-plan | Revised plan to v0.3 and closed all four v2 findings while preserving owner, gates, Phase 4a boundary, and P0-P6 order | feature brief; `docs/workflow/features/phase4a-control-plane-core/{design.md,api.md,test.md,dev_log.md}`; no review report/product/ADR/implementation-plan/dashboard/Git write | `NEEDS_REVIEW`; executable P2 identity prerequisite, exact sync wire, exact command attempt recovery, and corrected crypto/enrollment gates frozen; no self-approval | `feature-review v3` |
| 2026-07-21 02:48 PDT | Codex (GPT-5) as feature-review | Independently reviewed v0.3, confirmed closure of all four v2 findings, and rechecked P0-P6 executability, security boundaries, rollback, and verification gates | `docs/reviews/phase4a-control-plane-core/2026-07-21-feature-review-v3.md`; this Status Panel and one Work Log row only; no commit | `REVISE`; the authoritative P4 snapshot cannot encode its declared Device resource and lacks canonical page/final digest construction | `feature-plan` |
| 2026-07-21 02:53 PDT | Codex (GPT-5) as feature-plan | Revised plan to v0.4 and closed the sole v3 snapshot finding without changing P0-P6 order or Phase 4a scope | feature brief; `docs/workflow/features/phase4a-control-plane-core/{design.md,api.md,test.md,dev_log.md}`; no review report/product/ADR/implementation-plan/dashboard/Git write | `NEEDS_REVIEW`; Account-first typed resources and exact snapshot manifest/page/final integrity, replay, expiry, and commit gates frozen; no self-approval | `feature-review v4` |
| 2026-07-21 02:59 PDT | Codex (GPT-5) as feature-review | Independently reviewed v0.4, verified closure of the sole v3 snapshot finding, and regressed P0-P6 contracts, ordering, security boundaries, rollback, and gates | `docs/reviews/phase4a-control-plane-core/2026-07-21-feature-review-v4.md`; this Status Panel and one Work Log row only; no commit | `APPROVED`; repository-valid ready-to-build state with no material finding; Provider Gate none and Security Gate remains open | `feature-build P0` |
| 2026-07-21 09:26 PDT | Codex (GPT-5) as feature-build | Completed only approved P0: reconciled full-digest/six-group pinning, strict typed attestation over both digests, X25519/HKDF/HMAC/Ed25519 proof of possession, portable Vault/storage assertions and negatives, 4a/4b/5 boundaries, dependency provenance, and exact Go/TypeScript parity while preserving prior E2EE negatives | P0-owned authoritative docs and vector implementations plus this `dev_log.md`; no product package, migration, dependency lock, dashboard judgment, commit, push, or merge | `READY_FOR_VERIFY`; verifier SHA-256 `34da114390dec3e6c089313fe0e7c44dcb46f94af57458ee85004e01c0fa5ca8`; byte-identical direct output SHA-256 `53c206b6d9450028f57e50246ece147cf037281f2f6ff741d082a29fe8c7bfa5`; direct, project, CI, diff, secret, and scope gates pass; Security Gate remains open | independent `feature-verify P0` |
| 2026-07-21 09:37 PDT | Codex (GPT-5) as independent feature-verify | Inspected the complete P0 diff and receipt, independently reproduced the committed vectors and repository gates, then added hostile time validation, constant-time proof audit, exact dependency/source checks, scope/secret scans, and live branch/CI evidence checks; modified only this verdict and the verification report | `docs/reviews/phase4a-control-plane-core/2026-07-21-p0-feature-verify.md`; this Status Panel/Evidence Ledger/one Work Log row | `BLOCKED`; TypeScript accepts invalid calendar timestamps, its exchange HMAC comparison is not constant-time, and the revised vector lacks fresh Ubuntu/Windows execution receipts | original `feature-build` P0 writer fixes both vector defects, obtains exact-commit three-platform vector evidence, and restores `READY_FOR_VERIFY` |
| 2026-07-21 09:42 PDT | Codex (GPT-5) as feature-build | Cleared only the two independent P0 code findings: made TypeScript reject impossible UTC calendar values with microsecond-exact lifetime behavior matching Go, and replaced PoP HMAC `Buffer.equals` with a length-safe constant-time comparison; added shared invalid-date/hour/leap-boundary and wrong-content/short/long proof regressions | `docs/spikes/e2ee/{go/main.go,typescript/validate.mjs,verify.mjs}`; current result hashes in ADR/spike; this `dev_log.md`; verifier report unchanged; no product, dependency, dashboard judgment, commit, push, or merge | local correction gates pass with verifier `55bff1decd0b3419df4d43e32fe933e397a9167253c89f7a7d71552c178520f5` and byte-identical output `3ab9bbe3477ee64293d229c50b364b5c3721ae8190c6c443b33b2b585adf8dde`; status intentionally remains `BLOCKED` pending exact-commit native Ubuntu/macOS/Windows receipts | coordinator commits/pushes the scoped P0 result and returns three-platform receipts; writer then records receipts and restores `READY_FOR_VERIFY` |
| 2026-07-21 09:53 PDT | Codex (GPT-5) as feature-build | Recorded the exact-commit PR #32 remote receipts after the local corrections and stable provenance-link follow-up; confirmed all E2EE, project/build, license, DCO, and link jobs pass on one head | exact head `37d517bd6f9f5f70c8f21a9ac8e544b5a106a993`; this `dev_log.md` only; no implementation/report/commit/push mutation | `READY_FOR_VERIFY`; 10/10 remote checks pass, including native Ubuntu/macOS/Windows vectors and builds; verifier hash remains `55bff1decd0b3419df4d43e32fe933e397a9167253c89f7a7d71552c178520f5`; Security Gate remains open | independent `feature-verify P0` |
| 2026-07-21 09:58 PDT | Codex (GPT-5) as independent feature-verify v2 | Re-inspected the complete current P0 diff, independently closed all three prior findings with hostile time/HMAC probes, reran full relevant local gates, validated five provenance links, and queried PR #32 run/job/log evidence against one exact head; modified only this verdict and the v2 report | `docs/reviews/phase4a-control-plane-core/2026-07-21-p0-feature-verify-v2.md`; this Status Panel/Evidence Ledger/one Work Log row | `VERIFIED`; strict Go/TypeScript contract parity, constant-time HMAC path, native three-platform vector receipts, and P0 scope all pass; Security Gate remains open | `feature-build P1` |
| 2026-07-21 11:03 PDT | Codex (GPT-5) as feature-plan | Paused P1 at its safe uncommitted checkpoint, re-read the complete v0.4 authority, and revised only the five planning/state artifacts to v0.5 with decision-complete P2-P6 contracts plus matching executable tests; preserved verified P0 and every P1 implementation/generated change | feature brief; `docs/workflow/features/phase4a-control-plane-core/{design.md,api.md,test.md,dev_log.md}`; no implementation/review-report/dashboard/ADR/implementation-plan/commit/push write | `NEEDS_REVIEW`; full OpenAPI/generators must reconcile to v0.5 before P1 verification; Provider Gate none and Security Gate open; no self-approval | `feature-review v5` |
| 2026-07-21 11:10 PDT | Codex (GPT-5) as feature-review | Independently reviewed plan v0.5 against the complete feature authority, P1 full-contract checkpoint, and all P2-P6 origin/WebAuthn/enrollment/migration/mapping/capability/sync/command/browser/Desktop/privacy regressions; reproduced structural passes and the truthful Python-2.0 P1 license gate | `docs/reviews/phase4a-control-plane-core/2026-07-21-feature-review-v5.md`; this Status Panel and one Work Log row only; no plan/implementation/generated/dashboard/commit/push write | `REVISE`; architecture boundaries are sound, but exact P5 delivery/receipt, P3 activation package, P2 WebAuthn extension/options, Profile conflict, and one stale migration contract must be closed before P1 can generate v0.5 without invention | `feature-plan v0.6` |
| 2026-07-21 11:21 PDT | Codex (GPT-5) as feature-plan | Read v5 review completely and revised only the five planning/state artifacts to v0.6: replaced every legacy P5 wire DTO with exact offer/claim/ack/result/reconcile/query/cursor plus closed outcome/reservation/proof/receipt types, separated signed activation receipt from package/final ack, enumerated WebAuthn options/empty extensions, closed Profile conflict mutation/digests, and removed the P4 migration contradiction | feature brief; `docs/workflow/features/phase4a-control-plane-core/{design.md,api.md,test.md,dev_log.md}`; v5 report and all P1 implementation/generated/dependency files preserved; no commit/push | `NEEDS_REVIEW`; Findings 1-5 have normative schemas and executable negative/golden tests; P0 remains verified, Provider Gate none, Security Gate open, and Python-2.0 remains a truthful P1 verification blocker | `feature-review v6` |
| 2026-07-21 11:27 PDT | Codex (GPT-5) as feature-review | Independently re-read plan v0.6 and v5, confirmed all five v5 findings are closed at the API/test level, regressed the full Phase 4a boundaries, and ran structural/link/license checks while preserving every paused P1 file | `docs/reviews/phase4a-control-plane-core/2026-07-21-feature-review-v6.md`; this Status Panel and one Work Log row only; no plan/implementation/generated/dependency/dashboard/commit/push write | `REVISE`; one current-v0.6 cross-artifact contradiction remains between design's claimed closed P5 outcomes and api.md's exact SessionCommandOutcomeV1/CommonFailureCode oneOf; Python-2.0 remains only a P1 verification gate | `feature-plan v0.7` |
| 2026-07-21 11:30 PDT | Codex (GPT-5) as feature-plan | Read Feature Review v6 completely and revised only the five planning/state artifacts to v0.7: made design's P5 stable outcome contract byte-semantically identical to API's complete CommonFailureCode and SessionCommandOutcomeV1 unions, and added a cross-document stale-code/complete-union scan | feature brief; `docs/workflow/features/phase4a-control-plane-core/{design.md,api.md,test.md,dev_log.md}`; v6 report and every P1 implementation/generated/dependency file preserved; no commit/push | `NEEDS_REVIEW`; the sole v6 finding is closed without API schema, phase, or scope change; P0 remains verified, Provider Gate none, Security Gate open, and Python-2.0 remains a truthful P1 verification blocker | `feature-review v7` |
| 2026-07-21 11:35 PDT | Codex (GPT-5) as feature-review | Independently reviewed plan v0.7 and v6, byte-compared the complete design/API P5 outcome unions, verified the executable stale/complete-union regression, regressed all v5 findings and Phase 4a boundaries, and reran structural/link/license checks while preserving every paused P1 file | `docs/reviews/phase4a-control-plane-core/2026-07-21-feature-review-v7.md`; this Status Panel and exactly one Work Log row only; no plan/implementation/generated/dependency/dashboard/commit/push write | `APPROVED`; the sole v6 contradiction is closed, all v5 findings remain closed, P0 remains verified, P1 runtime remains health/ready/version only, and Python-2.0 remains solely a P1 verification gate | `feature-build P1` |
| 2026-07-21 12:22 PDT | Codex (GPT-5) as feature-build | Completed only approved P1: reconciled the complete v0.7 OpenAPI and deterministic Go/TypeScript generation; added exhaustive same-origin Protocol client and sole Web import point; implemented strict config/private storage, transactional/checksummed migrations, verified backups, health/readiness/version-only server lifecycle, UUIDv7/time/Base64url/cursor/idempotency/error/hostile-JSON foundations; removed the Python-2.0 dependency edge and the one-time reconciliation script | `api/openapi/`; `cmd/multidesk-server/`; `internal/controlplane/`; `internal/transport/`; `migrations/server/`; `packages/protocol/`; `apps/web/src/api/`; root dependency/tool scripts; this `dev_log.md`; no plan/review/dashboard-judgment/commit/push/merge write | `READY_FOR_VERIFY`; all focused, deterministic-generation, license, full Go/race/vet/cross-build, Protocol/Web/Desktop, project/CI/scaffold, DCO/diff/secret/scope gates pass; runtime has no P2+ handlers/state and Security Gate remains open | independent `feature-verify P1` |
| 2026-07-21 12:32 PDT | Codex (GPT-5) as independent feature-verify | Re-read the complete repository/role/plan v0.7 authority and P1 diff, reran deterministic generation plus local hostile/full/race/platform/structural gates, audited the runtime client and Windows DACL mask/owner boundary, and bound PR #32 native logs to the exact committed head; modified only this verdict report and the allowed Status Panel/Evidence Ledger/one Work Log row | `docs/reviews/phase4a-control-plane-core/2026-07-21-p1-feature-verify.md`; this Status Panel/Evidence Ledger/one Work Log row; no implementation/OpenAPI/generated/dependency/plan/dashboard/commit/push write | `BLOCKED`; clean macOS Web resolution and native Windows private-path tests fail, and the exhaustive client cannot express Enrollment pre-auth or preserve required generated inputs; P2 must not start | original `feature-build P1` writer clears Findings 1-4 and restores `READY_FOR_VERIFY` for independent re-verification |
| 2026-07-21 13:06 PDT | Codex (GPT-5) as feature-build | Cleared exactly the four P1 verifier findings: made Protocol/Web resolution source-based and clean-tree reproducible; routed real cross-platform fixtures and new database creation through strict private-path protection while adding native Windows DACL positive/negative coverage; parameterized and regenerated exact Enrollment pre-auth headers for all seven candidate operations and added mutually exclusive typed runtime construction; preserved generated required body/path/query semantics with compile/runtime regressions | `packages/protocol/{package.json,src/control-plane-client.ts,src/control-plane-client.test.ts,src/generated/control-plane-v1.ts}`; `api/openapi/control-plane-v1.yaml`; generated Go; `internal/controlplane/{privatefs_test.go,privatefs_windows_test.go,privatefs_unix_test.go,config_server_test.go,store.go,store_test.go}`; `scripts/{api/generate.mjs,scaffold/verify-layout.mjs}`; this `dev_log.md`; blocking verifier report unchanged; no P2/plan/commit/push/merge write | `READY_FOR_VERIFY`; clean install/no-dist Web proof, deterministic generation, focused/hostile/full/race/vet/platform compile, Protocol/Web/Desktop, project/CI/scaffold/license/DCO/secret/scope/diff gates pass; fresh exact-head native Windows/macOS evidence remains independent verifier/coordinator work | independent `feature-verify P1` |
| 2026-07-21 13:16 PDT | Codex (GPT-5) as independent feature-verify v2 | Reproduced all four prior findings against exact head in a truly fresh clone and through source/runtime/type audits, reran focused/full/race/vet/structural regression, and bound all ten PR #32 checks plus the failing native log to that head; modified only the v2 verdict report and allowed Status Panel/Evidence Ledger/one Work Log row | `docs/reviews/phase4a-control-plane-core/2026-07-21-p1-feature-verify-v2.md`; this Status Panel/Evidence Ledger/one Work Log row; no implementation/OpenAPI/generated/dependency/plan/dashboard/commit/push write | `BLOCKED`; prior Findings 1-4 are cleared, but Windows verified backup fails when a read-only backup handle is synced; 9/10 exact-head checks pass and P2 must not start | original `feature-build P1` writer repairs Windows backup durability without weakening private permissions, obtains a fresh exact-head 10/10 receipt, and restores `READY_FOR_VERIFY` |
| 2026-07-21 13:25 PDT | Codex (GPT-5) as feature-build | Cleared only the P1 v2 Windows backup durability finding: opened the already-protected `VACUUM INTO` backup with the minimum writable handle required by Windows `Sync`, and extended the existing prior-schema backup regression to prove protected read-write sync plus unchanged private permissions before integrity, foreign-key, version, and data restoration checks | `internal/controlplane/store.go`; `internal/controlplane/store_test.go`; this Status Panel/Evidence Ledger/one Work Log row; verifier v2 report unchanged; no P2/plan/dashboard judgment/commit/push/merge write | `READY_FOR_VERIFY`; focused/repeated/full/race/vet/cross-build, prior four-finding, deterministic API/license, project/CI/layout/format/DCO/diff/secret/scope/artifact gates pass locally; native Windows exact-head evidence remains independent | independent `feature-verify P1` obtains the fresh exact-head 10/10 receipt before P2 |
| 2026-07-21 13:50 PDT | Codex (GPT-5) as feature-build | Cleared only the exact-head `3b2aa28` P1 Windows tool-launch finding: removed the generator's package-manager subprocess, introduced a canonical no-shell Node CLI runner for the remaining Windows pnpm license operations, and wired six Windows/path/argv/fail-closed regressions into `api:verify` | `package.json`; `scripts/api/{generate.mjs,process-runner.mjs,process-runner.test.mjs,verify-tool-licenses.mjs}`; `scripts/ci/verify-licenses.mjs`; this Status Panel/Evidence Ledger/one Work Log row; no generated API/P2/plan/review/dashboard-judgment/commit/push/merge write | `READY_FOR_VERIFY`; all focused, deterministic generation, license, full scaffold, project/CI, Go/race/vet/platform, Protocol, DCO/diff/secret/scope/artifact gates pass locally; native Windows exact-head evidence remains independent | coordinator commits/pushes the scoped clearing, then independent `feature-verify P1` obtains the fresh exact-head 10/10 receipt before P2 |
| 2026-07-21 14:15 PDT | Codex (GPT-5) as feature-build | Cleared only exact job `88759763597`'s P1 Windows action-layout finding: replaced the stale bootstrap-sibling lookup with strict discovery of the pinned action's active `PNPM_HOME/bin` plus `global/v11`/versioned-store layout, exact repository pin and package metadata checks, canonical containment and uniqueness, while retaining `process.execPath`, argument arrays, and forced no-shell execution; expanded the protected regression from six to eight cases with the real stale-v11/active-v10 topology, canonical escape, and ambiguity negatives | `scripts/api/process-runner.mjs`; `scripts/api/process-runner.test.mjs`; this Status Panel/Evidence Ledger/one Work Log row; verifier reports unchanged; no P2/plan/generated API/dashboard judgment/commit/push/merge write | `READY_FOR_VERIFY`; focused 8/8, deterministic generation, API/full license, frozen install, complete scaffold, project/CI, Go full/race/vet/platform, Protocol, DCO/diff/no-shell/scope/artifact gates pass locally; native Windows exact-head evidence remains independent | coordinator commits/pushes the scoped v4 clearing, then independent `feature-verify P1` obtains a fresh exact-head 10/10 receipt before P2 |
| 2026-07-21 14:25 PDT | Codex (GPT-5) as independent feature-verify v3 | Independently audited the final clearing chain and exact committed source; reran focused/full/race/vet/type/runtime/generation/license/clean-Web/structural gates; rejected two interim native Windows defects; then inspected the final Windows Go→API→license→Web→Desktop scaffold log and bound all ten PR checks to one exact SHA; modified only this v3 report and allowed Status Panel/Evidence Ledger/one Work Log row | `docs/reviews/phase4a-control-plane-core/2026-07-21-p1-feature-verify-v3.md`; this Status Panel/Evidence Ledger/one Work Log row; no implementation/OpenAPI/generated/dependency/plan/dashboard/commit/push write | `VERIFIED`; all P1 acceptance criteria and prior findings are closed at exact head `6bbad01f17db7a40f0dc43bc45a41738f302640b`; Security Gate remains open and P2 has not started | `feature-build P2` |
| 2026-07-21 16:39 PDT | Codex (GPT-5) as feature-build | Implemented only approved P2 through a safe checkpoint: exact device schema-v7 backup/restore and 0008 identity migration; canonical remote identity/envelope/AAD prepare/prove/verify/activate; server bootstrap, WebAuthn, recovery, browser-session security, process lock, CLI commands, and memory-only responsive Web journeys; added restart/concurrency/CAS/counter/rate-limit/secret regressions and stopped before P3 | `apps/web/{index.html,src/}`; `cmd/multidesk*`; `internal/{app,controlplane,crypto,storage,transport,vault}`; `migrations/{device,server}`; `go.mod`; `go.sum`; this Status Panel/Evidence Ledger/Risks/one Work Log row; no plan/review/dashboard judgment/commit/push/merge | `BLOCKED`; all local implementation, race, deterministic-generation, Protocol/Web/Desktop, native/cross-build, project/CI/license/diff/secret/scope gates pass, but CSRF restart semantics, idempotent session/secret replay, session revision/idle-touch semantics, and the macOS-first acceptance matrix require v0.8 plan authority | `feature-plan v0.8`, then independent `feature-verify P2` |
| 2026-07-22 00:34 PDT | Codex (GPT-5) as feature-plan | Re-read the safe P2 checkpoint and revised only the five planning/state artifacts to v0.8: froze canonical-origin/generation-bound restart-safe CSRF, exact all-P2 endpoint idempotency with secret-nonreplay receipts and recovery actions, item-authoritative session/passkey revisions with coalesced idle touch, macOS-first Chrome/Safari/real Touch ID acceptance, Windows/Ubuntu CI-only gates, and Claude existing-subscription interactive-PTY isolation | feature brief; `docs/workflow/features/phase4a-control-plane-core/{design.md,api.md,test.md,dev_log.md}`; P2 implementation/checkpoint, generated files, review reports, dashboard judgment, `docs/IMPLEMENTATION_PLAN.md`, and Git state preserved | `NEEDS_REVIEW`; the three P2 protocol ambiguities and platform boundary are decision-complete with executable regressions; P0/P1 remain verified, Provider Gate none, Security Gate open, and no browser/product acceptance is claimed | independent `feature-review v8` |
| 2026-07-22 00:44 PDT | Codex (GPT-5) as independent feature-review v8 | Independently reviewed exact plan v0.8 and checkpoint `fc2a38e`, confirmed the macOS-first, CSRF, one-time-secret, per-item session, rollback, generated-delta, and P3+ isolation closures, and persisted two ranked idempotency-contract findings | `docs/reviews/phase4a-control-plane-core/2026-07-22-feature-review-v8.md`; this Status Panel/Evidence Ledger/one Work Log row only; no plan/implementation/generated/dependency/dashboard/Git write | `REVISE`; the formula/test disagree on cross-scope key reuse, actor placement, and raw-versus-canonical body identity, and the strict operation column omits ceremony-begin enum values | `feature-plan v0.9` |
| 2026-07-22 00:59 PDT | Codex (GPT-5) as feature-plan | Read Feature Review v8 completely and revised only the five planning/state artifacts to v0.9: replaced the cross-scope P2 formula with one global normalized-key primary domain and one actor-bearing canonical-JCS request identity; froze header/body/path/If-Match normalization, digest frames, constant-time mismatch behavior, DB uniqueness/indexes and restart/expiry cleanup; unified all thirteen endpoint, migration, receipt, OpenAPI/generated, store, and test operation values | feature brief; `docs/workflow/features/phase4a-control-plane-core/{design.md,api.md,test.md,dev_log.md}`; v8 review and all implementation/generated/dependency/dashboard/`docs/IMPLEMENTATION_PLAN.md`/Git state preserved | `NEEDS_REVIEW`; v8 Findings 1-2 are closed without reopening approved macOS-first, CSRF, secret-nonreplay, item/session revision, rollback, Claude, browser, or P3+ boundaries; P0/P1 remain verified, Provider Gate none, Security Gate open | independent `feature-review v9` |
| 2026-07-22 01:05 PDT | Codex (GPT-5) as independent feature-review v9 | Independently reviewed complete plan v0.9 and Feature Review v8, cross-checked the global normalized-key/JCS request-identity formulas, exact storage/index/error/restart/concurrency rules, and complete thirteen-operation enum, and regressed every approved v0.8 boundary | `docs/reviews/phase4a-control-plane-core/2026-07-22-feature-review-v9.md`; this Status Panel/Evidence Ledger/one Work Log row only; no plan/implementation/generated/dependency/dashboard/`docs/IMPLEMENTATION_PLAN.md`/Git write | `APPROVED`; v8 Findings 1-2 are closed without invention, all prior closures remain intact, P0/P1 remain verified, Provider Gate none, and Security Gate open | `feature-build P2` |
| 2026-07-22 04:56 PDT | Codex (GPT-5) as feature-build | Completed only approved P2 v0.9 implementation and local verification: closed restart-safe CSRF/global thirteen-operation idempotency/secret-nonreplay, item revision/touch, WebAuthn/recovery/session, backup/restore/private-storage, strict generated API/Protocol, source-gated Web/static runtime, Windows CI contracts, and fail-closed macOS Chrome/Safari/Touch ID receipt tooling; incorporated three independent audit rounds without starting P3 | P2-owned migration, `internal/{controlplane,storage,transport}`, OpenAPI/generated artifacts, Protocol/Web, CI scripts/workflow, receipt harness/template, and this state authority; base HEAD remains `4fa86ff2d5ba6afdacbabea3bd6f20fe9fc2d580`; no review report/dashboard judgment/commit/push/merge/live receipt | `BLOCKED`; every local focused/full/race/vet/build/cross-compile/API/Protocol/Web/Desktop/project/CI/license/diff/secret/scope/artifact gate passes, and independent local audits report no code blocker; exact-head native Windows Actions plus clean-SHA real Chrome/Safari and physical Safari Touch ID receipts remain mandatory | coordinator creates/pushes the final P2 implementation SHA, obtains native Windows and browser receipts, then returns them for a dev-log-only `READY_FOR_VERIFY` transition |
| 2026-07-22 05:28 PDT | Codex (GPT-5) as feature-build | Reproduced exact-head P2 CI run `29917919930` (`9205207b0a92c9f7a115563a90167760ca3c4c94`, PR merge `6753bdeb`) and cleared only its Ubuntu/Windows blockers plus the final independent Windows audit: fixed the six-microsecond bootstrap fixture while preserving 7/8/9-digit schema-before-auth rejection; normalized product `RemoteBootstrapService.Prepare` time before Vault persistence; replaced Windows-normalized on-disk ACE-flag negatives with provably nonzero raw ACL fixtures through the same production verifier; unified Device disk paths with the exact TokenUser owner plus TokenUser/SYSTEM DACL; made `.new` files exact through disk-only creation `SECURITY_ATTRIBUTES` and handle verification before their first write while preserving the endpoint-only Logon SID/Network-deny pipe policy; made four raw migration fixtures establish the private boundary; kept both concurrent stores alive until both `Open` calls return; and removed the false Windows skip from the platform daemon protocol test | `internal/app/{remote_bootstrap.go,remote_bootstrap_test.go}`; `internal/controlplane/{p2_http_security_test.go,privatefs_windows.go,privatefs_windows_test.go}`; `internal/device/{endpoint_windows.go,privatefs_unix.go,privatefs_windows.go,privatefs_windows_test.go,protocol_test.go}`; `internal/storage/{accounts_test.go,privatefs_windows_test.go,schema_v7_backup_windows.go,store_test.go}`; this Status Panel and the existing 05:28 Work Log row; no review report/dashboard judgment/commit/push/merge | `BLOCKED`; fixed/high-precision bootstrap, four migration, 20x concurrent-open, platform daemon, creation-time handle ACL, and unsafe-content/SDDL regressions pass; full Go/vet/race including app/build, Linux/Windows cross-build and Windows four-package test compile, deterministic API, Protocol 16/16, Web 8/8/assets, project/CI 7-check/15-action/17-receipt, links and license gates pass; fresh exact-head native Windows and clean-SHA browser/Touch ID evidence are still required | coordinator commits/pushes this scoped clearing, reruns exact-head native Windows CI, then returns that receipt together with the outstanding clean-SHA browser receipts |
| 2026-07-22 06:19 PDT | Codex (GPT-5) as feature-build | Reproduced exact run `29921105390` / Windows job `88926540545` at `ace3c15aab12a17751853099f622130a7d4491a3` and cleared only its Codex Windows private-boundary root causes: exact root/quarantine/staging/home validation, creation-time private create-only lock, atomic private refresh/manifest replacement, verify-before-adopt, fail-closed recovery/quarantine, truthful source/residue/same-file Provider fixtures, fail-independent verbose Windows P2 acceptance receipts, and exact whole-step command contracts that reject masked exits or extra PowerShell commands | `.github/workflows/ci.yml`; `internal/device/{privatefs_unix.go,privatefs_windows.go,privatefs_test.go,privatefs_windows_test.go}`; `internal/providers/codex/{materialization.go,materialization_test.go,vault_source_test.go}`; `scripts/ci/{verify-actions.mjs,test-gates.mjs}`; this Status Panel/Evidence Ledger/Risks/one Work Log row; no review report/plan/dashboard judgment/commit/push/merge | `BLOCKED`; Device create/replace/collision 20x and Codex materialization/official-residue/lock-recovery 10x plus focused/full/race/vet/cross-build and all API/Protocol/Web/project/CI/license/format/diff/eol gates pass locally; CI negative fixtures prove `; exit 0` and an added command fail the static contract; the exact failing tests are structurally closed without weakening `VerifyPrivateDirectory/File`, but only a new committed/pushed SHA and native Windows run can supply the required receipt; browser/Touch ID receipts remain missing | coordinator commits/pushes this exact scoped clearing, obtains Windows full-Go plus both P2 acceptance-step passes on that SHA, then returns it with the clean-SHA browser/Touch ID receipts |
| 2026-07-22 06:58 PDT | Codex (GPT-5) as feature-build | Cleared only the remaining native Windows SQLite sidecar concurrency race in Storage and Control Plane: confined bounded unsafe-to-exact waiting and disappear-as-benign handling to already-observed `-journal`/`-wal`/`-shm` paths; preserved stable directory/main-file fail-closed behavior and exact owner/DACL checks; added deterministic Windows converge/vanish/post-open/persistent-unsafe regressions; drained both concurrent results and registered all successful stores for cleanup before assertions | `internal/storage/{store.go,store_test.go,schema_v7_backup_windows.go,schema_v7_backup_unix.go,privatefs_windows_test.go}`; `internal/controlplane/{store.go,store_test.go,privatefs_windows_test.go}`; this Status Panel/Evidence Ledger/Risks/one Work Log row; no review/plan/dashboard judgment/commit/push/merge | `BLOCKED`; final focused/full-Go/20x concurrency/two-package race/vet/project/CI/diff and Windows amd64 CGO0 test compilation pass; native Windows has not run this uncommitted clearing and browser/Touch ID receipts remain missing | coordinator commits/pushes the scoped clearing, obtains fresh exact-head Windows full-Go plus both P2 acceptance-step receipts, then returns them with the clean-SHA browser/Touch ID receipts |
| 2026-07-22 14:46 PDT | Codex (GPT-5) as bug-fix | Repaired only the diagnosed Node 24 receipt-collector socket lifecycle defect: captured and fail-closed validated the TLS socket at the HTTPS response callback entrance, kept end-phase validation to exact JSON 200/body/commit, and added a real delayed keep-alive lifecycle regression plus the required TLS negatives without disabling keep-alive or relaxing trust | `scripts/acceptance/p2-browser-receipt.mjs`; `scripts/acceptance/p2-browser-receipt.test.mjs`; this Status Panel/Evidence Ledger/one Work Log row; no product implementation, plan/review/dashboard judgment, commit, push, or PR | scoped bugfix is `READY_FOR_VERIFY`; all focused, packaged, project, CI, syntax, license, link, and diff gates pass; feature P2 intentionally remains `BLOCKED` because clean-SHA browser/Touch ID and any still-unrecorded exact-head native Windows receipts remain external evidence | independent `bug-verify` for the receipt socket-lifecycle repair, then coordinator creates the exact-SHA receipt run |
| 2026-07-22 14:51 PDT | Codex (GPT-5) as independent bug-verify | Independently audited the two-file socket-lifecycle diff and reproduced the original Node 24 delayed keep-alive failure shape plus every required TLS negative; reran focused/full/package acceptance, syntax, project, CI, license/link, and diff gates; changed only this verifier verdict record and its report | `docs/reviews/phase4a-control-plane-core/2026-07-22-bug-verify.md`; this Status Panel/Evidence Ledger/exactly one Work Log row; no implementation/plan/dashboard judgment/commit/push/PR write | scoped bugfix `READY_TO_SHIP`; no finding or security-boundary regression; feature P2 remains `BLOCKED` on the pre-existing exact-head native Windows and clean-SHA real-browser/physical Touch ID receipts | `ship` the scoped bugfix when human-authorized, then coordinator collects the outstanding P2 receipts |
| 2026-07-22 14:59 PDT | Codex (GPT-5) as ship | Used the operator's explicit commit/push/PR authorization, rechecked exact scope/docs/licenses/security/version/rollback/DCO/test gates, created and pushed signed commit `a3bd87e`, and persisted this scoped receipt without changing dashboard judgment or claiming Phase 4a P2 completion | `a3bd87edd18a09bd1230d348a47ba16f0747cdf8`; `docs/reviews/phase4a-control-plane-core/2026-07-22-receipt-socket-ship-receipt.md`; this state authority; PR #32 | scoped bugfix `READY_TO_SHIP -> SHIPPED`; feature P2 remains `BLOCKED` on machine-verifiable secret-scan evidence plus fresh exact-head native Windows and clean-SHA real Chrome/Safari/physical Touch ID receipts; no merge/tag/release/publish/deploy | clear the recorded P2 evidence blockers before any P2 verification claim |
| 2026-07-22 15:41 PDT | Codex (GPT-5) as feature-build | Implemented the approved P2 machine-bound secret-scan evidence slice only: removed legacy journey scan attestations; bound manifest/context/tool/process/FIFO/reader/target state; added six-class full-tree scans, strict transfer/evidence allowlists, logical/raw SQLite assertions and secure-delete canaries; corrected public 32-byte digest length; redacted failure paths; documented exact operator order | OpenAPI/generated Go/TS; `internal/{controlplane,storage}` storage/tests; `scripts/acceptance/p2-browser-receipt*`; receipt template; `test.md`; this state authority; no P3, dashboard judgment, commit, push, PR, or live receipt | `BLOCKED`; all local focused/full/race/vet/cross-build/API/Protocol/project/CI/link/license/diff gates pass, but this dirty build cannot issue exact-SHA browser or machine-scan evidence and no manual Chrome/Safari/physical Touch ID journey was performed | coordinator creates/pushes the exact implementation SHA, obtains exact-head CI, then executes collect -> both manual journeys -> scan -> finalize before independent `feature-verify P2` |
| 2026-07-22 16:09 PDT | Codex (GPT-5) as feature-build | Cleared the complete machine-scan follow-up audit without broadening P2: hardened live process/image/input/open-DB/writable-FD/FIFO/TTY evidence, exact cryptographic DB/transfer/migration/schema bindings, non-content inventories, output-root exclusion, global RawURL generation, and remaining failure-log redaction | same approved P2 scan files plus existing Control Plane tests and this state authority; no P3/plan/review/dashboard judgment/commit/push/PR/live receipt | `BLOCKED`; receipt 34/34 twice, deterministic API, full Go/vet/race, native/Linux/Windows builds, Protocol, project/CI/license/link/syntax/diff gates all pass; only exact-SHA CI and real macOS Chrome/Safari/physical Touch ID/final scan receipts remain | coordinator commits/pushes after this build handoff, obtains exact-head CI, then runs the documented clean-SHA manual receipt sequence before independent `feature-verify P2` |
| 2026-07-22 16:25 PDT | Codex (GPT-5) as feature-build | Closed the final FIFO/PTY evidence audit: made real macOS FIFO holder discovery use global `lsof` canonical path+inode identity with exact writer/reader PID regression; narrowed the machine claim and schema to declared FIFO-to-live-TTY/process/root proof; added exact human Terminal-recording/scrollback attestations and documented PTY-master/Terminal/OS/operator exclusions | `scripts/acceptance/p2-browser-receipt.{mjs,test.mjs}`; receipt template; `test.md`; this Status Panel/Evidence Ledger/one Work Log row; no P3/plan/review/dashboard judgment/commit/push/PR/live receipt | `BLOCKED`; receipt 35/35, full Go/vet/race, native/Linux/Windows builds, deterministic API, Protocol and project/CI gates all pass; only exact-SHA CI and real macOS Chrome/Safari/physical Touch ID/final scan receipts remain | coordinator commits/pushes after this build handoff, obtains exact-head CI, then runs the documented clean-SHA `collect -> manual journeys -> scan -> finalize` sequence before independent `feature-verify P2` |
| 2026-07-22 16:34 PDT | Codex (GPT-5) as feature-build | Closed the final two P2 receipt findings: enforced byte-origin fatal UTF-8 strict JSON with JSON-only whitespace and null-prototype/dangerous-key rejection; added a real two-row six-root scan success and deterministic canary, unreadable, unstable, replacement, alias/hardlink, unexpected, empty, and mutation negatives that run beyond the process gate | `scripts/acceptance/p2-browser-receipt.{mjs,test.mjs}`; receipt template; `test.md`; this Status Panel/Evidence Ledger/one Work Log row; no P3/plan/review/dashboard judgment/commit/push/PR/live receipt | `BLOCKED`; focused tests and receipt 50/50 pass; full gates are rerun immediately after this append; only exact-SHA CI and real macOS Chrome/Safari/physical Touch ID/final scan receipts remain external | coordinator commits/pushes only after the completed build handoff, obtains exact-head CI, then executes the clean-SHA `collect -> manual journeys -> scan -> finalize` sequence before independent `feature-verify P2` |
| 2026-07-22 16:40 PDT | Codex (GPT-5) as feature-build | Closed the last detector-evidence audit gap by exposing only immutable sanitized scan counts to tests and requiring every target/secret canary pair to prove detector execution before the unchanged final rejection; retained a separate unexpected-log allowlist negative | `scripts/acceptance/p2-browser-receipt.{mjs,test.mjs}`; receipt template; `test.md`; this Status Panel/Evidence Ledger/one Work Log row; no secret/path disclosure, production CLI option, P3/plan/review/dashboard judgment/commit/push/PR/live receipt | `BLOCKED`; focused six-root matrix passes and the complete acceptance/project gates are rerun immediately after this append; only exact-SHA CI and real macOS Chrome/Safari/physical Touch ID/final scan receipts remain external | coordinator commits/pushes only after this final build Handoff, then obtains the documented external receipts before independent `feature-verify P2` |
| 2026-07-22 16:44 PDT | Codex (GPT-5) as feature-build | Appended the final API example-count truth correction without rewriting the historical 16:09 row: current maximal authoritative OpenAPI runs are 43=`225`, 86=`11`, and legacy 42/85=`0`; recorded that the completed detector audit remains closed | this Status Panel/Evidence Ledger/one Work Log row only; no implementation/generated/test/plan/dashboard judgment/commit/push/PR/P3 write | `BLOCKED`; `api:verify` and independent maximal-run guards pass; no local P2 finding remains, while exact-SHA CI/browser/Touch-ID/final-scan receipts remain external | coordinator may now take the completed feature-build Handoff and create the exact implementation SHA |
| 2026-07-22 17:03 PDT | Codex (GPT-5) as bug-fix | Repaired only the diagnosed native-Windows residue-test fixture boundary: reused the Control Plane exact-private test helper and made the Device test exercise production creation/protection of a new child directory; left the exact TokenUser+LocalSystem owner/DACL verifier and product behavior unchanged | `internal/controlplane/p2_secret_residue_test.go`; `internal/storage/p2_secret_residue_test.go`; this Status Panel/Evidence Ledger/Risks/one Work Log row; no P3, review report, dashboard judgment, commit, push, or PR write | scoped bugfix `READY_FOR_VERIFY`; focused/full Go, vet, Windows amd64 test compilation, project/CI/receipt/license/link/format/diff gates pass; feature P2 remains `BLOCKED` pending fresh exact-head native Windows and the existing real-browser/physical Touch ID/final-scan receipts | independent `bug-verify` for `phase4a-p2-windows-residue-test-private-root`, then coordinator commit/push and fresh exact-head Windows run |
| 2026-07-22 17:07 PDT | Codex (GPT-5) as independent bug-verify | Independently reproduced the original fixture boundary from exact-head source and public failed-job metadata, audited the minimal two-test repair and unchanged production Windows owner/DACL verifier, and reran focused 10x/full Go, vet, Windows CGO0 test/command compilation, project/CI/receipt/link/license/format/diff gates | `docs/reviews/phase4a-control-plane-core/2026-07-22-bug-verify-v2.md`; this Status Panel/Evidence Ledger/exactly one Work Log row; no implementation/plan/dashboard judgment/commit/push/PR write | scoped bugfix `READY_TO_SHIP`; no local finding or fail-closed boundary regression. Feature P2 remains `BLOCKED` because cross-compilation is not the required fresh exact-head native Windows receipt and the real Chrome/Safari/physical Touch ID/final scan are still outstanding | `ship` the scoped fixture repair under the existing human authorization, then require exact-head native Windows full-Go and P2 private-storage passes before any P2 verification claim |
| 2026-07-22 18:29 PDT | Codex (GPT-5) as feature-build/bug-fix | Reproduced the failed clean-head `collect`, repaired the receipt contract around the unchanged production process lock, removed broad server-database artifact exclusions, added fail-closed lock/runtime/context/finalize regressions including real macOS `lsof` plus exclusive contention, and documented the exact operator manifest and scan boundary | `scripts/acceptance/p2-browser-receipt.{mjs,test.mjs}`; `docs/workflow/features/phase4a-control-plane-core/{p2-browser-receipt-template.md,test.md,dev_log.md}`; no production lock, design, P3, dashboard judgment, commit, push, PR, browser, or persistent service write | scoped repair `READY_FOR_VERIFY`; focused 32/32, direct and packaged full receipt 56/56, Go process-lock, project/CI/license/link/syntax/diff gates pass. Feature P2 remains `BLOCKED`; this build does not claim browser/Touch-ID/final receipt completion | independent `bug-verify` of the process-lock receipt repair, then coordinator reruns clean-SHA `collect -> journeys -> scan -> finalize` |
| 2026-07-22 18:37 PDT | Codex (GPT-5) as independent bug-verify | Independently verified the unchanged production process lock and the new exact manifest/vnode/FD/global-holder/flock/freeze/scan/finalize bindings; reran focused/full receipt, ProcessLock Go, real macOS holder/probe, project/CI/syntax/link/license/diff gates and performed a semantic inventory audit | `docs/reviews/phase4a-control-plane-core/2026-07-22-bug-verify-v3.md`; this Status Panel/Evidence Ledger/exactly one Work Log row; no implementation/test/plan/dashboard judgment/commit/push/browser/P3 write | `BLOCKED`; all automated checks pass, but empty unknown runtime/backup/alternate-lock directories are invisible to unexpected-path and snapshot inventories, so the documented fail-closed receipt can falsely pass | `bug-fix` makes directory objects fail closed/inventory-bound and adds empty-directory plus add/remove/rename snapshot regressions, then returns for independent `bug-verify` |
| 2026-07-22 18:44 PDT | Codex (GPT-5) as bug-fix | Closed verifier v3 P1 only: made every non-excluded child directory a stable non-content inventory entry while preserving declared-root/excluded-subroot contracts, rejected unknown empty runtime/log/transfer/evidence directories, and bound directory metadata into both snapshots and finalize; added empty server-artifact/alternate-lock/backup directory, traversal mutation, symlink/private-mode, and signed-scan add/remove/rename regressions | `scripts/acceptance/p2-browser-receipt.{mjs,test.mjs}`; `docs/workflow/features/phase4a-control-plane-core/{p2-browser-receipt-template.md,test.md,dev_log.md}`; verifier report `2026-07-22-bug-verify-v3.md` and its verdict row preserved; no production lock, P3, dashboard judgment, commit, push, or browser write | scoped repair `READY_FOR_VERIFY`; focused directory matrix 25/25, direct and packaged receipt 63/63, Go ProcessLock, project/CI, 326 links, 6 pnpm groups, 418 Cargo licenses, syntax, and diff gates pass; no absolute path, cleartext directory name, or content digest is persisted | independent `bug-verify` for the directory-inventory repair, then coordinator may resume the exact clean-SHA receipt sequence |
| 2026-07-22 18:51 PDT | Codex (GPT-5) as independent bug-verify | Re-verified the repaired directory inventory and the unchanged process-lock manifest/vnode/FD/flock/global-holder/freeze/scan/finalize contract; audited receipt privacy and all four target-root fail-closed paths; reran focused/full/package receipt, ProcessLock Go 10x, project/CI, syntax/link/license/diff gates | `docs/reviews/phase4a-control-plane-core/2026-07-22-bug-verify-v4.md`; this Status Panel/Evidence Ledger/exactly one Work Log row; no implementation/test/plan/dashboard judgment/commit/push/browser/P3 write | scoped bugfix `READY_TO_SHIP`; v3 P1 closed and no new P0/P1 finding. Feature P2 remains `BLOCKED` on clean exact-SHA real Chrome/Safari/physical Touch ID/fresh scan/final receipt evidence | `ship` the scoped receipt repair, then coordinator executes the documented clean-SHA receipt sequence before independent `feature-verify P2` |
| 2026-07-23 14:49 PDT | Codex (GPT-5) as bug-fix | Repaired only the macOS `lsof -F` FIFO device-field mismatch: tolerate an absent FIFO `D` field, while requiring an exact match whenever it is present; retained exact FIFO kind/access/canonical-path/inode/owner/mode/link and holder/reader/TTY checks, and left regular-file/database/process-lock device checks unchanged. Added a real macOS FIFO `lsof` omission regression plus mock absent-device positive, wrong-present-device, missing-inode/path/access negatives; updated the receipt procedure and P2 test contract | `scripts/acceptance/p2-browser-receipt.{mjs,test.mjs}`; `docs/workflow/features/phase4a-control-plane-core/{p2-browser-receipt-template.md,test.md,dev_log.md}`; `node --check` both receipt files; focused real macOS `lsof` test; direct and packaged `acceptance:p2-browser:test` 64/64; `project:verify`; `ci:verify`; `git diff --check`; no production implementation, dashboard judgment, commit, push, browser journey, runtime, or PR write | scoped bugfix `READY_FOR_VERIFY`; all listed checks pass. Feature P2 remains `BLOCKED` on pre-existing clean-SHA real Chrome/Safari/physical Touch ID and scan/finalize receipt evidence | independent `bug-verify` for the FIFO device-field receipt repair |
| 2026-07-23 14:54 PDT | Codex (GPT-5) as independent bug-verify | Independently confirmed that only the declared FIFO `lsof` device field is conditional: a real native macOS writer stdout record is FIFO/write/exact-inode/exact-canonical-path with omitted `D`; mock absence passes, while a present wrong device and missing inode/path/access each fail. Rechecked that FIFO owner/0600/link, holder inventory, `/bin/cat` reader, TTY, and all REG database/process-lock device bindings remain strict; production paths have zero diff | `docs/reviews/phase4a-control-plane-core/2026-07-23-bug-verify-v5.md`; focused real-macOS and mock matrices; direct and packaged receipt suites 64/64; `npm run project:verify`; `npm run ci:verify`; Node syntax; `git diff --exit-code -- cmd internal apps api packages migrations`; `git diff --check`; no implementation/test/plan/dashboard judgment/commit/push/browser/P2 runtime/P3 write | scoped bugfix `READY_TO_SHIP`; no local finding. Feature P2 remains `BLOCKED` solely on the pre-existing clean exact-SHA Chrome/Safari/physical Touch ID/scan/finalize evidence | `ship` the scoped FIFO receipt repair under human authorization; then coordinator obtains the outstanding P2 evidence before independent `feature-verify P2` |
