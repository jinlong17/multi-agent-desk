# Feature state directories

Create one directory per canonical feature or bug slug. Copy `design.md`,
`api.md`, `test.md`, and `dev_log.md` from `docs/workflow/templates/`. Do not use
this README as a feature state record.
