# Development log: 多账号用量看板与显式调用

## Status Panel

| Field | Value |
|---|---|
| Workflow | `FEATURE_DEV` |
| Target | `multi-account-usage-control` |
| Title | `多账号用量看板与显式调用` |
| Owner Module | `provider` |
| Impacted Modules | `core, web, desktop, security, control-plane, project-system` |
| Current Phase | `VERIFY P1 RECONCILIATION` |
| Status | `VERIFIED` |
| Executor | `Codex (GPT-5) as independent feature-verify` |
| Updated | `2026-07-16 13:13 PDT` |
| Suggested Next | `refresh dashboard focus to VERIFIED, then revise and review the child Provider Spike assumptions before any separately approved P2 build` |
| Branch / Worktree | `codex/provider/multi-account-usage-control @ /Users/jinlong/Desktop/jinlong_project/agent-deck-worktrees/multi-account-usage-control` |
| Plan Version | `v0.3 + Phase 2 reconciliation overlay` |
| Provider Gate | `open — distinct-account Codex and Claude Spikes; Claude usage/policy evidence` |
| Security Gate | `open — credentials, login isolation, usage privacy, deletion/revocation and remote authorization` |

## Phase Plan

| Phase | Scope | Dependencies | Acceptance | Status |
|---|---|---|---|---|
| P0 Provider evidence (separate Spike units) | two distinct Codex Homes; two distinct Claude Config Dirs; quota schemas; Claude policy/monthly limits | approved Spike intakes; operator-owned accounts | both Spikes reach Security-reviewed decision; unsupported fields retain fallback | `SPIKE_READY in child units; may run alongside P1` |
| P1 manual registry and usage contracts | Account/Profile/alias domain; forward migration; CRUD/storage/IPC/CLI; generic stored Usage; Session preview binding; no real Provider launch | feature-review approval; Phase 1 shipped | 8+ mixed Profiles, pagination, migration/restart, alias/no-rotation/security tests, three-platform CI | `VERIFIED — original P1 acceptance and shipped Phase 2 Codex/Vault/runtime contracts both preserved after reconciliation` |
| P2 Codex multi-account vertical slice | managed per-credential `CODEX_HOME`; login/status/logout; app-server usage; explicit `@alias` run/shell | P1 verified; Codex Spike gate resolved; ADR 0014 | two different accounts isolated on Mac/Linux; official windows shown; real Session pinned; no second refresh writer | `GATED` |
| P3 Claude multi-account vertical slice | per-profile `CLAUDE_CONFIG_DIR`; login/status/logout; PTY; status-line usage; explicit alias | P1 verified; Claude Spike + policy gate resolved; ADR decision | two identities isolated; real PTY/session; 5h/7d truthful; monthly supported or explicit unavailable | `GATED` |
| P4 unified metadata and Web/Desktop dashboard | Control Plane metadata sync; Accounts/Usage pages; manual actions; responsive/accessibility | P2 and P3 verified or explicitly capability-gated; Phase 4a contracts | 6+/201 account UI, source/freshness/unknown states, no Provider browser secrets | `GATED` |
| P5 remote authorization and operations guide | target-device login/grant UX; Mac/Linux/Windows instructions; revocation; existing guide integration | P2/P3/P4 verified; CredentialGrant security gate; explicit branch integration | target-local fallback always works; supported grants are scoped/E2EE; guide/dashboard match truth | `GATED` |

Each product phase stops at `READY_FOR_VERIFY` for an independent verdict. P0
Spikes are separate workflow units and do not permit provider claims until their
own security review and feature-plan decision complete.

## Evidence Ledger

| Time | Phase | Command/evidence | Result | Artifact |
|---|---|---|---|
| 2026-07-15 00:58 PDT | INTAKE | `git fetch --all --prune`; local/remote branch and worktree audit; `/opt/homebrew/bin/gh` repo/PR queries | remote `main` `cb93c02`; Phase 1 shipped via merged PRs; Phase 0.5 Provider Spikes merged; current local user-guide branch is separate/dirty with untracked mirrors | Git/GitHub facts; no mutation beyond fetched refs |
| 2026-07-15 01:05 PDT | AUDIT | inspected Phase 1 domain/storage/CLI/provider packages on `origin/main` | only Fake Provider is runnable; no Account CRUD, real Profile CRUD, Usage collector, Provider adapter or product Web dashboard | `cmd/multidesk`; `internal/*`; migrations; Phase 1 log |
| 2026-07-15 01:11 PDT | PROVIDER | reviewed resolved Codex/Claude Spikes, ADR 0014/0016 and compatibility matrix | Codex usage methods proven for exact versions; Claude only one real identity; setup-token grant excluded | repo Provider evidence |
| 2026-07-15 01:18 PDT | OFFICIAL DOCS | fetched fresh Codex manual; reviewed Claude official env/auth/CLI/status-line/errors/Agent SDK/legal docs | isolated `CODEX_HOME` and credential store official; `CLAUDE_CONFIG_DIR` officially supports side-by-side accounts; Claude status-line provides 5h/7d fields; monthly machine field unknown; policy gate identified | official URLs in Feature Brief |
| 2026-07-15 01:24 PDT | LIVE LOCAL | sanitized local inventory and current Codex schema generation | Codex `0.144.2`, Claude `2.1.207`, one default login each; Codex schema has generic rate-limit windows/multi-buckets; no extra profile Homes detected | transient `/private/tmp` schema; no secret/identity values persisted |
| 2026-07-15 01:31 PDT | PLAN | module classification + canonical Feature Brief + phased contracts/tests + two Spike intakes | owner `provider`; P1 can proceed after review while Provider gates run; real/UI claims remain gated | feature documents and child Spike logs |
| 2026-07-15 02:32 PDT | BUILD P1 | implemented domain/storage/migration/authenticated IPC/CLI and Session binding; ran unit, integration, race, cross-build, daemon smoke, Web/Rust, workflow/dashboard/CI checks | approved metadata-only slice complete; six-account smoke passes; all Provider operations fail closed without a Session | `p1-as-built.md`; implementation and tests listed in Git diff |
| 2026-07-15 02:40 PDT | VERIFY P1 | `go test -count=1 ./...`; `go vet ./...`; targeted race suite; Darwin arm64, Linux amd64 and Windows amd64 storage test cross-compiles | pass; no automated regression in the tested matrix | independent command output; temporary test binaries outside the worktree |
| 2026-07-15 02:43 PDT | VERIFY P1 | workflow/dashboard/CI/link/layout/format/license verifiers; exact pnpm Web check; Rust format/locked check; `git diff --check origin/main` | pass; initial Web diagnostic omitted Node from PATH, exact pinned Node rerun passed; 208 Markdown files checked | independent command output; isolated Cargo target outside the worktree |
| 2026-07-15 02:46 PDT | VERIFY P1 | adversarial P1 database/daemon probes for Usage replay and public-Profile/Fake tuple isolation | fail: duplicate Usage replay tuple count `2`; legacy Fake command created a running Session with public Codex Account/Profile plus Fake Credential | `docs/reviews/multi-account-usage-control/2026-07-15-feature-verify.md`; sanitized temporary Device under `/private/tmp` |
| 2026-07-15 02:52 PDT | BUILD P1 | added transactional/unique Usage replay deduplication and full Session Account/Profile/Credential/Device/Provider/internal binding checks; added Store and authenticated native IPC regressions | focused, full Go, vet, race, Darwin/Linux/Windows storage compile and diff checks pass; duplicate replay remains one row and mixed Fake/Codex start creates no Session/process | implementation diff; targeted regression output |
| 2026-07-15 02:55 PDT | VERIFY P1 RERUN | replayed both original failures through the Store and an authenticated temporary daemon/CLI; inspected the fresh migration index and also ran the valid internal Fake path | pass: duplicate Usage ID safely leaves exactly one observation; mixed Fake Credential/public Codex Profile returns `conflict` with zero Sessions; valid internal Fake Profile reaches `running`; unique replay index present | independent targeted tests and sanitized temporary Device under `/private/tmp` |
| 2026-07-15 02:58 PDT | VERIFY P1 RERUN | full Go test/vet/race, three-platform storage cross-compile, migration/restart, workflow/dashboard/CI/link/layout/format/license, Web TypeScript, Rust format/check and diff validation | pass; dashboard static matched `READY_FOR_VERIFY` before this atomic verdict transition; 209 Markdown files checked | independent command output; isolated temporary build targets |
| 2026-07-16 12:40 PDT | PRE-REBASE PRESERVATION | fresh full Go test/vet/race; macOS/Linux/Windows command builds; workflow/dashboard/Actions/CODEOWNERS/fixtures/local links/licenses; diff integrity on the original `cb93c02` base | all pass; the verified 28-path P1 worktree is safe to preserve as one signed commit before reconciling onto final Phase 2 main | command output; pre-rebase branch/worktree audit |
| 2026-07-16 13:05 PDT | BUILD P1 RECONCILIATION | preserved original P1 as signed `1ac1f41`, rebased it as `eeeb558` onto final `origin/main@96ba36d`, resolved shared Account/Profile/Usage/domain/API surfaces, and moved the colliding migration to `0006_accounts_usage.sql` | Phase 2 Codex Account/Profile/Credential/Vault/Usage rows are preserved by an exact schema-v5 migration test; internal Fake rows remain private; no P2 Provider launch or credential Home is created | implementation diff; `p1-as-built.md`; reconciliation build report |
| 2026-07-16 13:05 PDT | BUILD P1 RECONCILIATION | `go test -count=1 ./...`; `go vet ./...`; `go test -count=1 -race ./...`; Darwin arm64/Linux amd64/Windows amd64 `go build ./cmd/...`; repeated migration/account tests; `git diff --check` | pass; code is ready for an independent reconciliation verdict before any push, merge, or later Provider phase | local command output |
| 2026-07-16 13:13 PDT | VERIFY P1 RECONCILIATION | inspected `96ba36d..a7ca66f`; repeated six migration/registry/security tests 10 times and both authenticated IPC boundaries 10 times; full Go/vet/race; three-OS command builds; Web/Rust; workflow/dashboard/governance/layout/link/license checks | pass; exact v5 Codex/Vault/Usage tuple survives migration 6; internal Fake is not public; alias launch remains fail-closed; no P2 credential/Home/process behavior added | `docs/reviews/multi-account-usage-control/2026-07-16-feature-verify.md`; independent command output |

## Risks and Blockers

- Original P1 is independently `VERIFIED` on `cb93c02`; both earlier blockers
  remain documented and were reproduced as cleared. The rebased reconciliation
  is `READY_FOR_VERIFY`, so no compatibility, ship, merge, or push claim is made
  until a fresh independent verdict.
- Stable Codex distinct-account support requires two real-account evidence; the
  existing Spike used the same account on two devices.
- Stable Claude multi-account support requires two real identities; existing
  ADR 0016 explicitly scoped v0.1 to one identity.
- Claude subscription login/rate-limit surfacing has an open Provider policy
  gate. No stable claim is permitted without an accepted applicability record.
- Claude monthly Agent SDK credit exists in official product documentation but
  no supported machine-readable remaining/reset contract is established.
- The cross-platform user operations guide is integrated on final remote
  `main`; this reconciliation preserves it and does not broaden its claims.
- P1 must not expand into real login or Provider process launch before P0/P2/P3
  gates.

## Work Log (append only)

| Time | Executor | Action | Files/commit | Result | Next |
|---|---|---|---|---|---|
| 2026-07-15 01:31 PDT | Codex (GPT-5) as feature-plan | Classified the operator's two P0 priorities into one Provider-owned feature, audited current remote/local implementation and official contracts, froze generic account/alias/usage/login boundaries, split five product phases, and opened two security-gated Provider Spike intakes without implementing product code | Feature Brief; `docs/workflow/features/multi-account-usage-control/{design,api,test,dev_log}.md`; two child Spike logs | `NEEDS_REVIEW`; P1 independently buildable only after approval; P2/P3/P4/P5 gated | independent `feature-review` |
| 2026-07-15 01:34 PDT | Codex root as operator-directed provider writer via `mad-dashboard-sync` | Applied the operator's explicit highest-priority direction to manual dashboard state, rebound focus to the parent feature and two exact Spike statuses, and preserved Phase 1 Ship plus all product/Provider/Security gates | `docs/workflow/project/dashboard-state.json`; this log | manual state names `NEEDS_REVIEW`/`SPIKE_READY` truthfully; generated refresh and verification required | run workflow/dashboard generation and independent feature-review |
| 2026-07-15 01:43 PDT | Codex (GPT-5) as independent feature-review | Reviewed P1 scope, contracts, migration, failure modes, security, Provider evidence, compatibility, tests, rollback, and phase ordering against the shipped Fake-only Device Kernel; modified only verdict surfaces | `docs/reviews/multi-account-usage-control/2026-07-15-feature-review.md`; this log | `REVISE`; Provider/Security phase gates are sound, but P1 cannot build until Fake backfill, Account/Profile/Credential/Home creation, and CRUD/pagination/delete semantics are frozen | `feature-plan` resolves ranked findings and re-enters `NEEDS_REVIEW` |
| 2026-07-15 01:51 PDT | Codex (GPT-5) as feature-plan | Resolved all ranked review findings: froze Fake as internal-only with deterministic Account and null-alias ID-preserving migration; made P1 create only Account + Profile with no credential/Home; specified revisioned update, keyset pagination, atomic deletion/tombstones and explicit P5 Grant deferral | `design.md`; `api.md`; `test.md`; this log | plan v0.3 `NEEDS_REVIEW`; no product code or Provider claim changed | independent `feature-review` |
| 2026-07-15 01:53 PDT | Codex (GPT-5) as independent feature-review | Re-reviewed plan v0.3 against all three ranked findings and the shipped Store/domain/auth/session baseline; verified migration, creation, CRUD, pagination, deletion, security, rollback and phase gates; modified only verdict surfaces | `docs/reviews/multi-account-usage-control/2026-07-15-feature-review.md`; this log; workflow/dashboard/link/diff checks | `APPROVED` for P1 only; prior findings closed; Provider and Security gates remain open | `feature-build` P1 manual registry and usage contracts |
| 2026-07-15 02:00 PDT | Codex (GPT-5) as feature-build via `mad-dashboard-sync` | Created the approved short-lived branch, entered P1 build without changing the approved gates, and rebound the operator dashboard focus to the exact `APPROVED` feature state | branch `codex/provider/multi-account-usage-control`; `docs/workflow/project/dashboard-state.json`; this log | P1 build active; real Provider and dashboard product phases remain gated | implement P1 then refresh generated facts and verify |
| 2026-07-15 02:32 PDT | Codex (GPT-5) as feature-build | Implemented the approved P1 Account/Profile/alias/Usage contracts, forward migration and Fake compatibility, revisioned CRUD/pagination/deletion, authenticated IPC/CLI, immutable Session account binding and fail-closed real-provider commands; added as-built truth and comprehensive tests | `internal/domain`; `internal/storage`; `internal/app`; `internal/device`; `cmd/multidesk`; `migrations/device`; `p1-as-built.md`; tests | `READY_FOR_VERIFY`; Go test/vet/race, three-OS storage compile, six-account daemon smoke, Web/Rust scaffold, workflow/dashboard/CI/link/license and diff checks pass; no credentials, Provider Home, real login, quota refresh, product UI, sync, push or ship | independent `feature-verify` P1 |
| 2026-07-15 02:46 PDT | Codex (GPT-5) as independent feature-verify | Inspected the full diff and build receipt, reran the relevant automated matrix, verified metadata-only creation and the gated real-Provider boundary, then executed targeted Usage replay and Session tuple probes; modified only the verification report and this state authority in one verdict step | `docs/reviews/multi-account-usage-control/2026-07-15-feature-verify.md`; this log | `BLOCKED`; automated checks pass, but replay dedup is absent and an explicit Fake start can bind a public Codex Profile to a Fake Credential and launch | `feature-build` clears both reproducible P1 conditions, adds regressions, and restores `READY_FOR_VERIFY` |
| 2026-07-15 02:49 PDT | Codex root as operator-directed provider writer via `mad-dashboard-sync` | Refreshed manual dashboard judgment immediately after the independent verdict so the highest-priority feature, both reproducible findings, and unchanged Provider/Security gates remain visible | `docs/workflow/project/dashboard-state.json`; this log | dashboard focus now binds to exact parent `BLOCKED` status; no implementation, Provider, remote, push or ship action included | `feature-build` clears both findings and reruns verification |
| 2026-07-15 02:52 PDT | Codex (GPT-5) as feature-build clearing writer | Added transactional plus unique-index Usage replay deduplication; made Session persistence reject any Account/Profile/Credential/Device/Provider/internal mismatch before process creation; added Store replay and authenticated native IPC mixed-Fake/Codex regressions; updated as-built truth | `migrations/device/0004_accounts_usage.sql`; `internal/storage/{accounts.go,accounts_test.go,repository.go}`; `internal/device/native_session_e2e_test.go`; `p1-as-built.md`; this log | restored `READY_FOR_VERIFY`; targeted/full Go, vet, race, three-platform storage compile and diff checks pass; both independent probes now fail closed | independent `feature-verify` rerun P1 |
| 2026-07-15 02:53 PDT | Codex root as operator-directed provider writer via `mad-dashboard-sync` | Rebound manual dashboard focus from the cleared `BLOCKED` verdict to exact `READY_FOR_VERIFY`, refreshed generated Git/feature facts, and reran workflow/dashboard/CI/link/license/layout/format verification | `docs/workflow/project/dashboard-state.json`; generated dashboard state; this log | all checks pass; dashboard names both cleared findings and keeps P2-P5, Provider, Security, remote, push and ship gates open | independent `feature-verify` rerun P1 |
| 2026-07-15 02:58 PDT | Codex (GPT-5) as independent feature-verify | Re-inspected the clearing diff and migration, replayed both original blockers through targeted tests and a fresh authenticated daemon, proved the valid internal Fake path still runs, and reran the proportionate regression/project matrix; modified only the verification report and this state authority in one verdict step | `docs/reviews/multi-account-usage-control/2026-07-15-feature-verify.md`; this log | `VERIFIED`; both prior findings are independently closed, while P2-P5 and Provider/Security gates remain open | refresh dashboard focus to `VERIFIED`; run both child `provider-spike` units before any separately approved P2 build |
| 2026-07-15 03:00 PDT | Codex root as operator-directed provider writer via `mad-dashboard-sync` | Rebound the highest-priority manual dashboard focus to the independent `VERIFIED` P1 verdict, named both child Provider Spikes as the next executable work, and kept every real-login/quota/UI/remote/Security gate explicit | `docs/workflow/project/dashboard-state.json`; generated dashboard state; this log | dashboard and feature authority agree: P1 foundation verified, full product not implemented, no push/merge/ship authorized | execute both child `provider-spike` units, then independently plan/review P2/P3 |
| 2026-07-16 12:40 PDT | Codex (GPT-5) as operator-authorized provider reconciliation writer | Re-audited the previously uncommitted P1 work against its original Phase 1 base and reran the full local regression/platform/governance matrix before history movement | all 28 modified/untracked paths in this worktree; command output; this log | P1 remains `VERIFIED` on its original base and is ready for a signed preservation commit; no Phase 2 compatibility claim is made yet | commit exact P1 state, then rebase onto final `origin/main` and resolve migration/schema overlap without starting P2 |
| 2026-07-16 13:05 PDT | Codex (GPT-5) as operator-authorized provider reconciliation writer | Preserved original P1, rebased onto final Phase 2 main, unified the overlapping domain/storage/IPC/CLI contracts, introduced version-6 forward migration with Phase 2 row/Vault preservation coverage, and reran the full Go/race/three-OS build matrix | signed preservation `1ac1f41`, rebased `eeeb558`; implementation, migration, tests, docs and reconciliation report | `READY_FOR_VERIFY`; original P1 behavior and shipped Phase 2 behavior pass locally; P2-P5 and Provider/Security gates stay open | independent `feature-verify` of reconciliation, then operator dashboard refresh |
| 2026-07-16 13:10 PDT | Codex root as operator-directed provider writer via `mad-dashboard-sync` | Rebound manual dashboard judgment to the exact reconciliation state and child Spike gates, regenerated workflow/dashboard facts, and ran Go, race, three-OS command builds, Web, Desktop/Rust, layout, Actions, CODEOWNERS, fixtures, links and license checks | `dashboard-state.json`; generated workflow/dashboard facts; command output; this log | all checks pass; `READY_FOR_VERIFY` is reflected without claiming P2, merge, ship, release or deployment | independent `feature-verify` of the reconciliation |
| 2026-07-16 13:13 PDT | Codex (GPT-5) as independent feature-verify | Re-inspected the reconciliation commit against original P1 acceptance and final Phase 2 contracts; repeated migration, registry, replay, tuple-isolation and authenticated IPC probes; reran the full platform/project matrix; modified only this verdict authority and the verification report | `docs/reviews/multi-account-usage-control/2026-07-16-feature-verify.md`; this log | `VERIFIED`; no reconciliation finding remains, while P2-P5 and Provider/Security gates stay open | operator dashboard refresh, then feature-plan/review updates for the child Spike assumptions |
