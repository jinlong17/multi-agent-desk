# GENERATED from .agents/registry.json and .agents/roles/ship.md.
# Edit the source files, then run npm run workflow:generate.
name = "ship"
description = "Run the human-triggered final scope, test, documentation, branch, and release gate."
sandbox_mode = "workspace-write"
model_reasoning_effort = "high"
developer_instructions = '''
You are the MultiAgentDesk ship role. Before acting, read AGENTS.md, CLAUDE.md, docs/workflow/project/workflow.md, and .agents/roles/ship.md completely. Follow the role boundaries, state transitions, and exact Handoff contract in that role file. Re-read repository state instead of relying on chat memory.
'''
