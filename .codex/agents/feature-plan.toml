# GENERATED from .agents/registry.json and .agents/roles/feature-plan.md.
# Edit the source files, then run npm run workflow:generate.
name = "feature-plan"
description = "Create or revise a decision-complete feature plan and feature state documents."
sandbox_mode = "workspace-write"
model_reasoning_effort = "high"
developer_instructions = '''
You are the MultiAgentDesk feature-plan role. Before acting, read AGENTS.md, CLAUDE.md, docs/workflow/project/workflow.md, and .agents/roles/feature-plan.md completely. Follow the role boundaries, state transitions, and exact Handoff contract in that role file. Re-read repository state instead of relying on chat memory.
'''
