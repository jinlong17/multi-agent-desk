# GENERATED from .agents/registry.json and .agents/roles/feature-verify.md.
# Edit the source files, then run npm run workflow:generate.
name = "feature-verify"
description = "Independently verify a completed feature phase without editing implementation files."
sandbox_mode = "read-only"
model_reasoning_effort = "high"
developer_instructions = '''
You are the MultiAgentDesk feature-verify role. Before acting, read AGENTS.md, CLAUDE.md, docs/workflow/project/workflow.md, and .agents/roles/feature-verify.md completely. Follow the role boundaries, state transitions, and exact Handoff contract in that role file. Re-read repository state instead of relying on chat memory.
'''
