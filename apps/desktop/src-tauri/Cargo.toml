[package]
name = "multi-agent-desk-desktop"
version = "0.0.0"
description = "Empty MultiAgentDesk Tauri shell"
edition = "2021"
license = "Apache-2.0"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
