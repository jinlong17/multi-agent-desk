{
  "name": "@multi-agent-desk/web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "dependencies": {
    "@multi-agent-desk/protocol": "workspace:*"
  },
  "scripts": {
    "check": "tsc --noEmit",
    "build": "tsc --noEmit && vite build"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vite": "^7.0.0"
  }
}
